<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/2022/06/12/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<span id="more"></span>

<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>Python中簡單的網絡編程</title>
    <url>/2022/08/22/code/python/net/</url>
    <content><![CDATA[<p>Python中簡單的網絡編程</p>
<div class="note info no-icon"><p>十分重要的是， Python的吞吐其實并不優秀。但是Python是一個十分重要工具語言，它可以做到的是可以簡化測試編程的壓力。</p>
</div>
<span id="more"></span>
<details class="note warning no-icon"><summary><p>需要注意的點</p>
</summary>
<div class="tabs" id="warntabs"><ul class="nav-tabs"><li class="tab active"><a href="#warntabs-1">問題</a></li><li class="tab"><a href="#warntabs-2">一些Features</a></li></ul><div class="tab-content"><div class="tab-pane active" id="warntabs-1"><ul>
<li>比較困難的是 TCP 如果是在自己的機器上測試TCP，可能不太容易模擬多個不同的客戶端</li>
</ul></div><div class="tab-pane" id="warntabs-2"></div></div></div>
</details>

<h1 id="Server-相關的代碼"><a href="#Server-相關的代碼" class="headerlink" title="Server 相關的代碼"></a>Server 相關的代碼</h1><details class="note info no-icon"><summary><p>server的參考代碼</p>
</summary>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\ --- tcp 服务端程序 server.py ---</span></span><br><span class="line"><span class="comment">#\ 导入 socket 库</span></span><br><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"><span class="comment">#\ 主机地址，表示绑定本机所有网络接口ip地址</span></span><br><span class="line"><span class="comment">#\ 等待客户端连接</span></span><br><span class="line">IP = <span class="string">&#x27;127.0.0.1&#x27;</span></span><br><span class="line"><span class="comment">#\ 端口号</span></span><br><span class="line">PORT = <span class="number">5000</span></span><br><span class="line"><span class="comment">#\ 定义一次从socket缓冲区最多读入512个字节数据，可自定义</span></span><br><span class="line">BUFLEN = <span class="number">512</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#\ 实例化一个socket对象</span></span><br><span class="line"><span class="comment">#\ 参数AF_INET表示该socket网络层使用IP协议</span></span><br><span class="line"><span class="comment">#\ 参数SOCK_STREAM表示该socket传输层使用tcp协议</span></span><br><span class="line">listenSocket = socket(AF_INET, SOCK_STREAM)</span><br><span class="line"></span><br><span class="line"><span class="comment">#\ socket绑定地址和端口</span></span><br><span class="line">listenSocket.bind((IP, PORT))</span><br><span class="line"></span><br><span class="line"><span class="comment">#\ 使socket处于监听状态，等待客户端的连接请求</span></span><br><span class="line"><span class="comment">#\ 参数5表示， 最多接5个等待连接的客户端，可自定义</span></span><br><span class="line">listenSocket.listen(<span class="number">5</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;服务器启动成功，在<span class="subst">&#123;PORT&#125;</span>端口等待客户端连接...&#x27;</span>)</span><br><span class="line"></span><br><span class="line">dataSocket, addr = listenSocket.accept()</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;接受一个客户端连接：&#x27;</span>, addr)</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    <span class="comment">#\ 尝试读取对方发送的信息</span></span><br><span class="line">    <span class="comment">#\ BUFLEN 指定从接收缓冲里最多读取多少字节</span></span><br><span class="line">    recved = dataSocket.recv(BUFLEN)</span><br><span class="line"></span><br><span class="line">    <span class="comment">#\ 如果返回空bytes, 表示对方关闭了连接</span></span><br><span class="line">    <span class="comment">#\ 退出循环，结束消息收发</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> recved:</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">#\ 读取的字节数据是bytes类型，需要解码为字符串</span></span><br><span class="line">    info = recved.decode()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&#x27;收到对方信息：<span class="subst">&#123;info&#125;</span>&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment">#\ 发送的数据类型必须是bytes,所以要编码</span></span><br><span class="line">    dataSocket.send(<span class="string">f&#x27;服务端接收到了信息<span class="subst">&#123;info&#125;</span>&#x27;</span>.encode())</span><br><span class="line"><span class="comment">#\ dataSocket.close()</span></span><br><span class="line"><span class="comment">#\ listenSocket.close()</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

</details>

<h1 id="Client-相關的代碼"><a href="#Client-相關的代碼" class="headerlink" title="Client 相關的代碼"></a>Client 相關的代碼</h1><details class="note info no-icon"><summary><p>client的參考代碼</p>
</summary>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\  --- tcp 客户端程序 client.py ---</span></span><br><span class="line"><span class="keyword">from</span> socket <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line">IP = <span class="string">&#x27;127.0.0.1&#x27;</span></span><br><span class="line">SERVER_PORT = <span class="number">5000</span></span><br><span class="line">BUFLEN = <span class="number">512</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#\ 实例化一个socket对象，指明协议</span></span><br><span class="line">dataSocket = socket(AF_INET, SOCK_STREAM)</span><br><span class="line"></span><br><span class="line"><span class="comment">#\ 连接服务器socket</span></span><br><span class="line">dataSocket.connect((IP, SERVER_PORT))</span><br><span class="line"></span><br><span class="line"><span class="comment">#\ 以下部分传入数据的格式仅供参考，这里示范传入字符串</span></span><br><span class="line"><span class="comment">#\ k但是客户端和服务端之间的交流为字节码，需要字符串与字节码的转化</span></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    <span class="comment">#\ 从终端读入用户输入的字符串（可自定义是否为字符串）</span></span><br><span class="line">    toSend = <span class="built_in">input</span>(<span class="string">&#x27;&gt;&gt; &#x27;</span>)</span><br><span class="line">    <span class="keyword">if</span> toSend == <span class="string">&#x27;exit&#x27;</span>:</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">    <span class="comment">#\ 发送信息，也要编码为 byes</span></span><br><span class="line">    dataSocket.send(toSend.encode())</span><br><span class="line"></span><br><span class="line">    <span class="comment">#\ 等待接收服务段的消息</span></span><br><span class="line">    recved = dataSocket.recv(BUFLEN)</span><br><span class="line">    <span class="comment">#\ 如果返回空bytes，表示对方关闭了连接</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> recved:</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line">    <span class="comment">#\ 打印读取的信息</span></span><br><span class="line">    <span class="built_in">print</span>(recved.decode())</span><br><span class="line"></span><br><span class="line"><span class="comment">#\ dataSocket.close()</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

</details>]]></content>
      <categories>
        <category>python</category>
        <category>network</category>
      </categories>
      <tags>
        <tag>network</tag>
      </tags>
  </entry>
  <entry>
    <title>Algorithm 的一些匯總</title>
    <url>/2022/08/22/code/algorithm/note/</url>
    <content><![CDATA[<p>Algorithm</p>
<span id="more"></span>

<h1 id="編程技巧"><a href="#編程技巧" class="headerlink" title="編程技巧"></a>編程技巧</h1><h1 id="綫性表"><a href="#綫性表" class="headerlink" title="綫性表"></a>綫性表</h1><h2 id="鏈表"><a href="#鏈表" class="headerlink" title="鏈表"></a>鏈表</h2><blockquote>
<p>節點的代碼</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> _Tp&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">listNode</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">listNode</span> *prev;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">listNode</span> *next;</span><br><span class="line">    _Tp data;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T = <span class="type">int</span>&gt;</span><br><span class="line"><span class="keyword">using</span> _node = <span class="keyword">struct</span> listNode&lt;T&gt;;</span><br></pre></td></tr></table></figure>

<h3 id="鏈表的逆序"><a href="#鏈表的逆序" class="headerlink" title="鏈表的逆序"></a>鏈表的逆序</h3><details class="note default no-icon"><summary><p>從頭部插入</p>
</summary>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">_node* <span class="title">Reverse</span><span class="params">(_node *head)</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="keyword">if</span> (<span class="literal">nullptr</span> == head) <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    _node *prev = <span class="literal">nullptr</span>, *next = <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="keyword">while</span> (<span class="literal">nullptr</span> != head)</span><br><span class="line">    &#123;   next = head-&gt;next;</span><br><span class="line">        head-&gt;next = prev;</span><br><span class="line">        prev = head;</span><br><span class="line">        head = next; &#125;</span><br><span class="line">    <span class="keyword">return</span> prev; &#125;</span><br></pre></td></tr></table></figure>
</details>

<details class="note default no-icon"><summary><p>雙指針插入</p>
</summary>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">_node* <span class="title">Reverse</span><span class="params">(_node *head)</span></span></span><br><span class="line"><span class="function"></span>&#123;   _node *cur = head, *prev = <span class="literal">nullptr</span>, *temp = <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="keyword">while</span> (<span class="literal">nullptr</span> != cur)</span><br><span class="line">    &#123;   temp = cur-&gt;next;</span><br><span class="line">        cur-&gt;next = prev;</span><br><span class="line">        prev = cur;</span><br><span class="line">        cur = temp; &#125;</span><br><span class="line">    <span class="keyword">return</span> prev; &#125;</span><br></pre></td></tr></table></figure>
</details>

<details class="note default no-icon"><summary><p>遞歸實現</p>
</summary>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">_node* <span class="title">Reverse</span><span class="params">(_node *head)</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="keyword">if</span> (<span class="literal">nullptr</span> == head || <span class="literal">nullptr</span> == head-&gt;next) <span class="keyword">return</span> head;</span><br><span class="line">    _node *newHead = <span class="built_in">Reverse</span>(head-&gt;next);</span><br><span class="line">    head-&gt;next-&gt;next = head;</span><br><span class="line">    head-&gt;next = <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="keyword">return</span> newHead; &#125;</span><br></pre></td></tr></table></figure>
</details>

<h1 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h1><h2 id="字符串函數"><a href="#字符串函數" class="headerlink" title="字符串函數"></a>字符串函數</h2><figure class="highlight cpp"><figcaption><span>strncpy</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">char</span>* <span class="title">strncpy</span><span class="params">(<span class="type">char</span> *dest, <span class="type">const</span> <span class="type">char</span> *src, <span class="type">size_t</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="built_in">assert</span>(<span class="literal">nullptr</span> != dest &amp;&amp; <span class="literal">nullptr</span> != src &amp;&amp; n &gt;= <span class="built_in">strlen</span>(src))</span><br><span class="line">    <span class="type">char</span> *_p = dest;</span><br><span class="line">    <span class="keyword">while</span> ((*_p++ = *src++) != <span class="string">&#x27;\0&#x27;</span>);</span><br><span class="line">    <span class="keyword">return</span> dest; &#125;</span><br></pre></td></tr></table></figure>

<h2 id="字符串應用"><a href="#字符串應用" class="headerlink" title="字符串應用"></a>字符串應用</h2><h3 id="KMP-的算法"><a href="#KMP-的算法" class="headerlink" title="KMP 的算法"></a>KMP 的算法</h3><p>KMP 算法，基本思想是，當出現不匹配時，就能知曉一部分文本的内容（因爲在匹配失敗之前它們已經和模式相匹配）。我們可以利用這些信息避免將指針回退到所有這些已知的字符之前。這樣當出現不匹配時，可以提前判斷如何重新開始查找，而這種判斷只取決於模式本身。</p>
<details class="note default no-icon"><summary><p>使用 next 數組的 kmp 算法</p>
</summary>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">compute_prefix</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pattern, <span class="type">int</span> next[])</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="keyword">constexpr</span> <span class="type">int</span> m = <span class="built_in">strlen</span>(pattern);</span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>,  j = <span class="number">-1</span>;</span><br><span class="line">    next[<span class="number">0</span>] = j;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">1</span>; i &lt; m ; ++i)</span><br><span class="line">    &#123;   <span class="keyword">while</span> (j &gt; <span class="number">-1</span> &amp;&amp; pattern[j + <span class="number">1</span>] != pattern[i]) j = next[j];</span><br><span class="line">        <span class="keyword">if</span> (pattern[i] == pattern[j + <span class="number">1</span>]) ++j;</span><br><span class="line">        next[i] = j; &#125; &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">kmp</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *text, <span class="type">const</span> <span class="type">char</span> *pattern)</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="type">const</span> <span class="type">int</span> n = <span class="built_in">strlen</span>(text), m = <span class="built_in">strlen</span>(pattern);</span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>, j = <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="number">0</span> == n &amp;&amp; <span class="number">0</span> == m) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="number">0</span> == m) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> *next = (<span class="type">int</span>*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(<span class="type">int</span>) * m);</span><br><span class="line">    <span class="built_in">compute_prefix</span>(pattern, next);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; n; ++i)</span><br><span class="line">    &#123;   <span class="keyword">while</span> (j &gt; <span class="number">-1</span> &amp;&amp; pattern[j + <span class="number">1</span>] != text[i]) j = next[j];</span><br><span class="line">        <span class="keyword">if</span> (text[i] == pattern[j + <span class="number">1</span>]) ++j;</span><br><span class="line">        <span class="keyword">if</span> (j == m - <span class="number">1</span>)</span><br><span class="line">        &#123;   <span class="built_in">free</span>(next);</span><br><span class="line">            <span class="keyword">return</span> i - j; &#125; &#125;</span><br><span class="line">    <span class="built_in">free</span>(next);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>; &#125;</span><br><span class="line"><span class="comment">// UNIT_TEST(kmp_test)</span></span><br><span class="line"><span class="comment">// &#123;   char test[] = &quot;ABC ABCDAB ABCDABCDABDE&quot;;</span></span><br><span class="line"><span class="comment">//     char pattern[] = &quot;ABCDABD&quot;;</span></span><br><span class="line"><span class="comment">//     char *ch = text;</span></span><br><span class="line"><span class="comment">//     int i = kmp(text, pattern);</span></span><br><span class="line"><span class="comment">//     ASSERT(i &gt;= 0, &quot;matched @: %s\n&quot;, ch + i); &#125;</span></span><br></pre></td></tr></table></figure>
</details>

<h3 id="Boyer-Moore-的算法"><a href="#Boyer-Moore-的算法" class="headerlink" title="Boyer-Moore 的算法"></a>Boyer-Moore 的算法</h3><details class="note default no-icon"><summary><p>Boyer-Moore 算法</p>
</summary>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> ASCII_SIZE 256</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">pre_right</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pattern, <span class="type">int</span> right[])</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="type">const</span> <span class="type">int</span> m = <span class="built_in">strlen</span>(pattern);</span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; ASCII_SIZE; ++i) right[i] = <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; m; ++i) right[(<span class="type">unsigned</span> <span class="type">char</span>)pattern[i]] = i; &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">suffixes</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *pattern, <span class="type">int</span> suff[])</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="type">const</span> <span class="type">int</span> m = <span class="built_in">strlen</span>(pattern);</span><br><span class="line">    <span class="type">int</span> f = <span class="number">0</span>, g = <span class="number">0</span>, i = <span class="number">0</span>;</span><br><span class="line">    suff[m - <span class="number">1</span>] = m;</span><br><span class="line">    g = m - <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = m - <span class="number">2</span>; i &gt;= <span class="number">0</span>; --i)</span><br><span class="line">    &#123;   <span class="keyword">if</span> (i &gt; g &amp;&amp; suff[i + m - <span class="number">1</span> - f] &lt; i - g)</span><br><span class="line">        &#123;   suff[i] = suff[i + m - <span class="number">1</span> - f]; &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;   <span class="keyword">if</span> (i &lt; g)</span><br><span class="line">            &#123;   g = i; &#125;</span><br><span class="line">            f = i;</span><br><span class="line">            <span class="keyword">while</span> (g &gt;= <span class="number">0</span> &amp;&amp; pattern[g] == pattern[g + m - <span class="number">1</span> - f])</span><br><span class="line">            &#123;   --g; &#125;</span><br><span class="line">            suff[i] = f - g; &#125; &#125; &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">pre_gs</span><span class="params">(<span class="type">const</span> <span class="type">char</span> pattern[], <span class="type">int</span> gs[])</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="type">const</span> <span class="type">int</span> m = <span class="built_in">strlen</span>(pattern);</span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>, j = <span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> *suff = (<span class="type">int</span>*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(<span class="type">int</span>) * (m + <span class="number">1</span>));</span><br><span class="line">    <span class="built_in">suffixes</span>(pattern, suff);</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; m; ++i) gs[i] = m;</span><br><span class="line">    j = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = m - <span class="number">1</span>; i &gt;= <span class="number">0</span>; --i)</span><br><span class="line">    &#123;   <span class="keyword">if</span> (i + <span class="number">1</span> == suff[i])</span><br><span class="line">        &#123;   <span class="keyword">for</span> (; j &lt; m - <span class="number">1</span> - i; ++j)</span><br><span class="line">            &#123;   <span class="keyword">if</span> (m == gs[j])</span><br><span class="line">                &#123;   gs[j] == m - <span class="number">1</span> -i; &#125; &#125; &#125; &#125;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt;= m - <span class="number">2</span>; ++i)</span><br><span class="line">    &#123;   gs[m - <span class="number">1</span> - suff[i]] = m - <span class="number">1</span> - i; &#125;</span><br><span class="line">    <span class="built_in">free</span>(suff); &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">boyer_moore</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *text, <span class="type">const</span> <span class="type">char</span> *pattern)</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="type">const</span> <span class="type">int</span> n = <span class="built_in">strlen</span>(text), m = <span class="built_in">strlen</span>(pattern);</span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>, j = <span class="number">0</span>, right[ASCII_SIZE];</span><br><span class="line">    <span class="type">int</span> *gs = (<span class="type">int</span>*)<span class="built_in">malloc</span>(<span class="built_in">sizeof</span>(<span class="type">int</span>) * (m + <span class="number">1</span>));</span><br><span class="line">    <span class="built_in">pre_right</span>(pattern, right);</span><br><span class="line">    <span class="built_in">pre_gs</span>(pattern, gs);</span><br><span class="line">    j = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (j &lt;= n - m)</span><br><span class="line">    &#123;   <span class="keyword">for</span> (i = m - <span class="number">1</span>; i &gt;= <span class="number">0</span> &amp;&amp; pattern[i] == text[i + j]; --i)</span><br><span class="line">        &#123;   <span class="keyword">if</span> (i &lt; <span class="number">0</span>)</span><br><span class="line">            &#123;   <span class="built_in">free</span>(gs);</span><br><span class="line">                <span class="keyword">return</span> j; &#125;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;   <span class="type">const</span> <span class="type">int</span> max = gs[i] &gt; right[(<span class="type">unsigned</span> <span class="type">char</span>)text[i + j]] - m + <span class="number">1</span> + i ? gs[i] : i - right[(<span class="type">unsigned</span> <span class="type">char</span>)text[i + j]]</span><br><span class="line">                j += max; &#125; &#125;</span><br><span class="line">        <span class="built_in">free</span>(gs);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>; &#125; &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// UNIT_TEST(boyer_moore_test)</span></span><br><span class="line"><span class="comment">// &#123;   char test[] = &quot;ABC ABCDAB ABCDABCDABDE&quot;;</span></span><br><span class="line"><span class="comment">//     char pattern[] = &quot;ABCDABD&quot;;</span></span><br><span class="line"><span class="comment">//     char *ch = text;</span></span><br><span class="line"><span class="comment">//     int i = boyer_moore(text, pattern);</span></span><br><span class="line"><span class="comment">//     ASSERT(i == 17); &#125;</span></span><br></pre></td></tr></table></figure>
</details>


<h1 id="棧和隊列"><a href="#棧和隊列" class="headerlink" title="棧和隊列"></a>棧和隊列</h1><h1 id="樹"><a href="#樹" class="headerlink" title="樹"></a>樹</h1><h1 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h1><h2 id="折半查找"><a href="#折半查找" class="headerlink" title="折半查找"></a>折半查找</h2><details class="note default no-icon"><summary><p>折半查找</p>
</summary>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> _Tp&gt;</span><br><span class="line"><span class="function">_Tp <span class="title">binary_swarch</span><span class="params">(<span class="type">const</span> _Tp a[], <span class="type">const</span> <span class="type">size_t</span> n, <span class="type">const</span> _Tp x)</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="type">size_t</span> _left = <span class="number">0</span>, right = n - <span class="number">1</span>, mid = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">whlie</span> (left &lt;= right)</span><br><span class="line">    &#123;   mid = left + (right - left) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span> (x &gt; a[mid])</span><br><span class="line">        &#123;   left = mid + <span class="number">1</span>; &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (x &lt; a[mid])</span><br><span class="line">        &#123;   right = mid - <span class="number">1</span>; &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;   <span class="keyword">return</span> mid; &#125; &#125;</span><br><span class="line">    <span class="keyword">return</span> -(left + <span class="number">1</span>); &#125;</span><br></pre></td></tr></table></figure>
</details>


<h1 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h1><h2 id="插入排序"><a href="#插入排序" class="headerlink" title="插入排序"></a>插入排序</h2><h3 id="直接插入排序"><a href="#直接插入排序" class="headerlink" title="直接插入排序"></a>直接插入排序</h3><p>直接插入排序的基本思想是： 把數組 a[n] 中待排序的 n 個元素看成為一個有序表和一個無序表，開始時有序表中只包含一個元素 a[0]， 無序表中包含 n - 1 個元素 a [1…(n - 1)] ，排序過程中每次從無序中取出第一個元素，把它插入到有序表中的適當的位置，使之成爲新的有序表，這樣經過 n - 1 次插入后，無序表就變爲空表，有序表中就包含了全部 n 個元素，至此排序完畢。在有序表中尋找插入位置是采用從后向前的順序查找的方法。</p>
<details class="note default"><summary><p>直接插入排序的示例</p>
</summary>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> _Tp&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">straight_insertion_sort</span><span class="params">(_Tp a[], <span class="type">const</span> <span class="type">size_t</span> start, <span class="type">const</span> <span class="type">size_t</span> end)</span></span></span><br><span class="line"><span class="function"></span>&#123;   _Tp temp;</span><br><span class="line">    <span class="type">size_t</span> i = <span class="number">0</span>, j = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = start + <span class="number">1</span>; i &lt; end; ++i)</span><br><span class="line">    &#123;   temp = a[i];</span><br><span class="line">        <span class="keyword">for</span> (j = i - <span class="number">1</span>; temp &lt; a[j] &amp;&amp; j &gt;= start; --j)</span><br><span class="line">        &#123;   a[j + <span class="number">1</span>] = a[j]; &#125;</span><br><span class="line">        a[j + <span class="number">1</span>] = temp; &#125; &#125;</span><br></pre></td></tr></table></figure>
</details>


<h3 id="折半插入排序"><a href="#折半插入排序" class="headerlink" title="折半插入排序"></a>折半插入排序</h3><p>在查找插入位置時，若改爲折半查找，就是折半插入排序。</p>
<details class="note default"><summary><p>折半插入排序的示例</p>
</summary>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> _Tp&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">binary_insertion_sort</span><span class="params">(_Tp a[], <span class="type">const</span> <span class="type">size_t</span> start, <span class="type">const</span> <span class="type">size_t</span> end)</span></span></span><br><span class="line"><span class="function"></span>&#123;   _Tp temp;</span><br><span class="line">    <span class="type">size_t</span> i = <span class="number">0</span>, j = <span class="number">0</span>, left = <span class="number">0</span>, right = <span class="number">0</span>, mid = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = start + <span class="number">1</span>; i &lt; end; ++i)</span><br><span class="line">    &#123;   temp = a[i];</span><br><span class="line">        left = start;</span><br><span class="line">        right = i - <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">while</span> (left &lt;= right)</span><br><span class="line">        &#123;   mid = (left + right) / <span class="number">2</span>;</span><br><span class="line">            <span class="keyword">if</span> (temp &lt; a[mid])</span><br><span class="line">            &#123;   right = mid - <span class="number">1</span>; &#125;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;   left = mid + <span class="number">1</span>; &#125; &#125;</span><br><span class="line">        <span class="keyword">for</span> (j = i - <span class="number">1</span>; j &gt;= left; --j)</span><br><span class="line">        &#123;   a[j + <span class="number">1</span>] = a[j]; &#125;</span><br><span class="line">        a[left] = temp; &#125; &#125;</span><br></pre></td></tr></table></figure>
</details>

<h3 id="希爾插入排序"><a href="#希爾插入排序" class="headerlink" title="希爾插入排序"></a>希爾插入排序</h3><p>從對直接插入排序的分析得知，其算法時間複雜度為 $O(n^{2})$，但是，若待排序記錄序列為“正序”時，其時間複雜度可提高至 $O(n)$。由此可設想，若待排序記錄序列按關鍵字“基本有序”，即序列中具有下列特性</p>
<p>$$<br>R_{i}.key &lt; max {R_{j}.key}, j &lt; j<br>$$</p>
<p>的記錄較少時，直接插入排序的效率就可大大提高，從另一方面來看，由於直接插入排序算法簡短，則在 n 值很小時效率也比較高。希爾排序正式從這兩點分析出發對直接插入排序進行改進得到的一種插入排序方法。</p>
<p>希爾排序的基本思想是： 設待排序元素序列有 n 個元素，首先取一個整數 $ gap &#x3D; \lfloor \frac{n}{3} \rfloor + 1 $ 作爲間隔， 將全部蒜素分爲 gap 個子序列，所有距離爲 gap 的元素放在同一個子序列中，在每一個子序列中分別進行直接插入排序。然後縮小間隔 gap， 取 $ gap &#x3D; \rfloor \frac{gap}{3} \lfloor + 1 $，重複上述的子序列劃分和排序工作，直到最後取 $ gap &#x3D; 1 $，將所有元素放在同一個序列中排序爲止。</p>
<details class="note default"><summary><p>希爾插入排序的示例</p>
</summary>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> _Tp&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">shell_insert</span><span class="params">(_Tp a[], <span class="type">const</span> <span class="type">size_t</span> start, <span class="type">const</span> <span class="type">size_t</span> end, <span class="type">const</span> <span class="type">size_t</span> gap)</span></span></span><br><span class="line"><span class="function"></span>&#123;   _Tp temp;</span><br><span class="line">    <span class="type">size_t</span> i = <span class="number">0</span>, j = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = start + gap; i &lt; end; ++i)</span><br><span class="line">    &#123;   temp = a[i];</span><br><span class="line">        <span class="keyword">for</span> (j = i - gap; temp &lt; a[j] &amp;&amp; j &gt;= start; j -= gap)</span><br><span class="line">        &#123;   a[j + gap] = a[j]; &#125;</span><br><span class="line">        a[j + gap] = temp; &#125; &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> _Tp&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">shell_sort</span><span class="params">(_Tp a[], <span class="type">const</span> <span class="type">size_t</span> start, <span class="type">const</span> <span class="type">size_t</span> end)</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="type">size_t</span> gap = end - start;</span><br><span class="line">    <span class="keyword">while</span> (gap &gt; <span class="number">1</span>)</span><br><span class="line">    &#123;   gap = gap / <span class="number">3</span> + <span class="number">1</span>;</span><br><span class="line">        <span class="built_in">shell_insert</span>(a, start, end, gap); &#125; &#125;</span><br></pre></td></tr></table></figure>
</details>


<h2 id="交換排序"><a href="#交換排序" class="headerlink" title="交換排序"></a>交換排序</h2><h3 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h3><p>快速排序的基本思想是任取待排序元素序列中的某個元素作爲基準（通常會選擇第一個元素），按照該元素的關鍵字大小，將整個元素序列劃分為左右兩個子序列：左側子序列中所有元素的關鍵字都小於基準元素的關鍵字，右側序列中所有元素的關鍵字都大於或等於基準元素的關鍵字，基準元素則排在兩個子序列中間（這也是該元素最終應該安放的位置）。然後分別對這兩個子序列重複實施上述算法，直到所有的元素都排在相應位置爲止。</p>
<details class="note default"><summary><p>快速排序的示例</p>
</summary>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> _Tp&gt;</span></span><br><span class="line"><span class="function"><span class="type">size_t</span> <span class="title">partition</span><span class="params">(_Tp a[], <span class="type">const</span> <span class="type">size_t</span> start, <span class="type">const</span> <span class="type">size_t</span> end)</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="type">size_t</span> i = start, j = end - <span class="number">1</span>;</span><br><span class="line">    <span class="type">const</span> _Tp pivot = a[i];</span><br><span class="line">    <span class="keyword">while</span> (i &lt; j)</span><br><span class="line">    &#123;   <span class="keyword">while</span> (i &lt; j &amp;&amp; a[j] &gt;= pivot) --j;</span><br><span class="line">        a[i] = a[j];</span><br><span class="line">        <span class="built_in">whlie</span> (i &lt; j &amp;&amp; a[i] &lt;= pivot) ++i;</span><br><span class="line">        a[j] = a[i]; &#125;</span><br><span class="line">    a[i] = pivot;</span><br><span class="line">    <span class="keyword">return</span> i; &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> _Tp&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">quick_sort</span><span class="params">(_Tp a[], <span class="type">const</span> <span class="type">size_t</span> start, <span class="type">const</span> <span class="type">size_t</span> end)</span></span></span><br><span class="line"><span class="function"></span>&#123;   <span class="keyword">if</span> (start &lt; end - <span class="number">1</span>)</span><br><span class="line">    &#123;   <span class="type">const</span> <span class="type">size_t</span> pivot_pos = <span class="built_in">partition</span>(a, start, end);</span><br><span class="line">        <span class="built_in">quick_sort</span>(a, start, pivot_pos);</span><br><span class="line">        <span class="built_in">quick_sort</span>(a, pivot_pos + <span class="number">1</span>, end); &#125; &#125;</span><br></pre></td></tr></table></figure>
</details>


<h1 id="暴力枚舉"><a href="#暴力枚舉" class="headerlink" title="暴力枚舉"></a>暴力枚舉</h1><h1 id="廣度優先搜索"><a href="#廣度優先搜索" class="headerlink" title="廣度優先搜索"></a>廣度優先搜索</h1><h1 id="深度優先搜索"><a href="#深度優先搜索" class="headerlink" title="深度優先搜索"></a>深度優先搜索</h1><h1 id="分治法"><a href="#分治法" class="headerlink" title="分治法"></a>分治法</h1><h1 id="貪心法"><a href="#貪心法" class="headerlink" title="貪心法"></a>貪心法</h1><h1 id="動態規劃"><a href="#動態規劃" class="headerlink" title="動態規劃"></a>動態規劃</h1><h1 id="圖"><a href="#圖" class="headerlink" title="圖"></a>圖</h1><h1 id="數學方法與常見模型"><a href="#數學方法與常見模型" class="headerlink" title="數學方法與常見模型"></a>數學方法與常見模型</h1><h1 id="大整數運算"><a href="#大整數運算" class="headerlink" title="大整數運算"></a>大整數運算</h1><h1 id="基礎功能"><a href="#基礎功能" class="headerlink" title="基礎功能"></a>基礎功能</h1>]]></content>
      <categories>
        <category>algorithm</category>
      </categories>
      <tags>
        <tag>algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>設計精巧的 Typelist</title>
    <url>/2022/08/22/code/cpp/loki/typelist/</url>
    <content><![CDATA[<p>設計精巧的 Typelist</p>
<span id="more"></span>

<h1 id="使用-Typelist"><a href="#使用-Typelist" class="headerlink" title="使用 Typelist"></a>使用 Typelist</h1><h2 id="Typelist-的定義"><a href="#Typelist-的定義" class="headerlink" title="Typelist 的定義"></a>Typelist 的定義</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span> <span class="title class_">U</span>&gt; <span class="keyword">struct</span> <span class="title class_">Typelist</span> &#123; <span class="keyword">typedef</span> T Head; <span class="keyword">typedef</span> U Tail; &#125;;</span><br></pre></td></tr></table></figure>

<h3 id="對於-Typelist-的補充"><a href="#對於-Typelist-的補充" class="headerlink" title="對於 Typelist 的補充"></a>對於 Typelist 的補充</h3><h4 id="Nulltype-的定義"><a href="#Nulltype-的定義" class="headerlink" title="Nulltype 的定義"></a>Nulltype 的定義</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">NullType</span> &#123;&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="Int2Type-的定義"><a href="#Int2Type-的定義" class="headerlink" title="Int2Type 的定義"></a>Int2Type 的定義</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="type">int</span> v&gt; <span class="keyword">struct</span> <span class="title class_">Int2Type</span> &#123; <span class="keyword">enum</span> &#123; value = v &#125;； &#125;；</span><br></pre></td></tr></table></figure>


<h4 id="Define-的產生"><a href="#Define-的產生" class="headerlink" title="Define 的產生"></a>Define 的產生</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> TYPELIST_1(T1) Typelist<span class="string">&lt;T1, NullType&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TYPELIST_2(T1, T2) Typelist<span class="string">&lt;T1, TYPELIST_1(T2)&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TYPELIST_3(T1, T2, T3) Typelist<span class="string">&lt;T1, TYPELIST_2(T2, T3)&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TYPELIST_4(T1, T2, T3, T4) Typelist<span class="string">&lt;T1, TYPELIST_3(T2, T3, T4)&gt;</span></span></span><br><span class="line"><span class="comment">// ... TYPELIST_50</span></span><br></pre></td></tr></table></figure>

<h3 id="Typelist-的長度"><a href="#Typelist-的長度" class="headerlink" title="Typelist 的長度"></a>Typelist 的長度</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt; <span class="keyword">class</span> <span class="title class_">TList</span>&gt; <span class="keyword">struct</span> <span class="title class_">Length</span>;</span><br><span class="line"><span class="keyword">template</span> &lt;&gt; <span class="keyword">struct</span> <span class="title class_">Length</span>&lt;NullType&gt; &#123; <span class="keyword">enum</span> &#123; value = <span class="number">0</span> &#125;; &#125;; </span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span> <span class="title class_">U</span>&gt; <span class="keyword">struct</span> <span class="title class_">Lentgh</span>&lt; Typelist&lt;T, U&gt;&gt; &#123; <span class="keyword">enum</span> &#123; value = <span class="number">1</span> + Lenngth&lt;U&gt;::value &#125;; &#125;;</span><br></pre></td></tr></table></figure>

<h3 id="Typelist-的定位"><a href="#Typelist-的定位" class="headerlink" title="Typelist 的定位"></a>Typelist 的定位</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>&gt; <span class="keyword">struct</span> <span class="title class_">TypeAt</span>&lt;Typelist&lt;Head, Tail&gt;, <span class="number">0</span>&gt; &#123; <span class="keyword">typedef</span> Head Result; &#125;；</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>, <span class="type">unsigned</span> <span class="type">int</span> i&gt; <span class="keyword">struct</span> <span class="title class_">TypeAt</span>&lt;Typelist&lt;Head, Tail&gt;, i&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> <span class="keyword">typename</span> TypeAt&lt;Tail, i - <span class="number">1</span>&gt;::Result Reslut;</span><br></pre></td></tr></table></figure>

<h3 id="Typelist-的查找"><a href="#Typelist-的查找" class="headerlink" title="Typelist 的查找"></a>Typelist 的查找</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">TList</span>, <span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">IndexOf</span>;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">IndexOF</span>&lt;NullType, T&gt; &#123; <span class="keyword">enum</span> &#123; value = <span class="number">-1</span> &#125;; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Tail</span>, <span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">IndexOf</span>&lt;Typelist&lt;T, Tail&gt;, T&gt; &#123; <span class="keyword">enum</span> &#123; value = <span class="number">0</span> &#125;; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>, <span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">class</span> <span class="title class_">IndexOf</span>&lt;Typelist&lt;Head, Tail&gt;, T&gt;</span><br><span class="line">&#123; <span class="keyword">enum</span> &#123; temp = IndexOf&lt;Tail, T&gt;::value &#125;;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">enum</span> &#123; value = temp == <span class="number">-1</span> ? <span class="number">-1</span> : <span class="number">1</span> + temp &#125;; &#125;;</span><br></pre></td></tr></table></figure>

<h3 id="Typelist-附加元素"><a href="#Typelist-附加元素" class="headerlink" title="Typelist 附加元素"></a>Typelist 附加元素</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">TList</span>, <span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">Append</span>;</span><br><span class="line"><span class="keyword">template</span> &lt;&gt; <span class="keyword">struct</span> <span class="title class_">Append</span>&lt;NullType, NullType&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> NullType Result; &#125;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">Append</span>&lt;NullType, T&gt;</span><br><span class="line">&#123; <span class="function"><span class="keyword">typedef</span> <span class="title">TYPELIST_1</span><span class="params">(T)</span> Result</span>; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Append</span>&lt;NullType, Typelist&lt;Head, Tail&gt;&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> Typelist&lt;Head, Tail&gt; Result; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>,  <span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Append</span>&lt;Typelist&lt;Head, Tail&gt;, T&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> Typelist&lt;Head, <span class="keyword">typename</span> Append&lt;Tail, T&gt;::Result&gt; Result; &#125;;</span><br></pre></td></tr></table></figure>

<h3 id="Typelist-移除元素"><a href="#Typelist-移除元素" class="headerlink" title="Typelist 移除元素"></a>Typelist 移除元素</h3><h4 id="定位刪除"><a href="#定位刪除" class="headerlink" title="定位刪除"></a>定位刪除</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">TList</span>, <span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">Erase</span>;</span><br><span class="line">tempalte &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">Erase</span>&lt;NullType, T&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> Tail Result; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>&gt; <span class="keyword">struct</span> <span class="title class_">Erase</span>&lt;Typelist&lt;T, Tail&gt;, T&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> Tail Result; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>, <span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Erase</span>&lt;Typelist&lt;Head, Tail&gt;, T&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> Typelist&lt;Head, <span class="keyword">typename</span> Erase&lt;Tail, T&gt;::Result&gt; Result; &#125;;</span><br></pre></td></tr></table></figure>

<h4 id="全部刪除"><a href="#全部刪除" class="headerlink" title="全部刪除"></a>全部刪除</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">TList</span>, <span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">EraseAll</span>;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">EraseAll</span>&lt;NullType, T&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> NullType Result; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">EraseAll</span>&lt;Typelist&lt;T, Tail&gt;, T&gt;</span><br><span class="line"><span class="comment">// Go all the way down the list removing the type.</span></span><br><span class="line">&#123; <span class="keyword">typedef</span> <span class="keyword">typename</span> EraseAll&lt;Tail, T&gt;::Result Result; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>, <span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">EraseAll</span>&lt;Typelist&lt;Head, Tail&gt;, T&gt;</span><br><span class="line"><span class="comment">// Go all the way down the list removing the type.</span></span><br><span class="line">&#123; <span class="keyword">typedef</span> Typelist&lt;Head, <span class="keyword">typename</span> EraseAll&lt;Tail, T&gt;::Result Result; &#125;;</span><br></pre></td></tr></table></figure>

<h3 id="Typelist-去重"><a href="#Typelist-去重" class="headerlink" title="Typelist 去重"></a>Typelist 去重</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">TList</span>&gt; <span class="keyword">struct</span> <span class="title class_">NoDuplicates</span>;</span><br><span class="line"><span class="keyword">template</span> &lt;&gt; <span class="keyword">struct</span> <span class="title class_">NoDuplicates</span>&lt;NullType&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span>  NullType Result; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">NoDuplicates</span>&lt;Typelist&lt;Head, Tail&gt;&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> <span class="keyword">typename</span> NoDuplicates&lt;Tail&gt;::Result L1;</span><br><span class="line">  <span class="keyword">typedef</span> <span class="keyword">typename</span> Erase&lt;L1, Head&gt;::Result L2;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">typedef</span> Typelist&lt;Head, L2&gt; Result; &#125;;</span><br></pre></td></tr></table></figure>

<h3 id="Typelist-替換"><a href="#Typelist-替換" class="headerlink" title="Typelist 替換"></a>Typelist 替換</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">TList</span>, <span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span> <span class="title class_">U</span>&gt; <span class="keyword">struct</span> <span class="title class_">Replace</span>;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span> <span class="title class_">U</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Replace</span>&lt;NullType, T, U&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> NullType Result; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>, <span class="keyword">class</span> <span class="title class_">U</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Replace</span>&lt;Typelist&lt;T, Tail&gt;, T, U&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> Typelist&lt;U, Tail&gt; Result; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>, <span class="keyword">class</span> <span class="title class_">T</span>, <span class="keyword">class</span> <span class="title class_">U</span>&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Replace</span>&lt;Typelist&lt;Head, Tail&gt;, T, U&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> Typelist&lt;Head, <span class="keyword">typename</span> Replace&lt;Tail, T, U&gt;::Result&gt; Result; &#125;;</span><br></pre></td></tr></table></figure>

<h2 id="Typelist-的使用"><a href="#Typelist-的使用" class="headerlink" title="Typelist 的使用"></a>Typelist 的使用</h2><h3 id="類的深度探測"><a href="#類的深度探測" class="headerlink" title="類的深度探測"></a>類的深度探測</h3><h4 id="MostDerived"><a href="#MostDerived" class="headerlink" title="MostDerived"></a>MostDerived</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">TList</span>, <span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">MostDerived</span>;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">MostDerived</span>&lt;NullType, T&gt;</span><br><span class="line">&#123; typdef T Result; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>, <span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">class</span> <span class="title class_">MostDerived</span>&lt;Typelist&lt;Head, Tail&gt;, T&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> <span class="keyword">typename</span> MostDerived&lt;Tail, T&gt;::Result Candidate;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">typedef</span> <span class="keyword">typename</span> Select&lt;<span class="built_in">SUPPERSUBCLASS</span>(Candidate, Head), Head, Candidate&gt;::Result Result; &#125;;</span><br></pre></td></tr></table></figure>


<h4 id="DerivedToFront"><a href="#DerivedToFront" class="headerlink" title="DerivedToFront"></a>DerivedToFront</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">struct</span> <span class="title class_">DerivedToFront</span>;</span><br><span class="line"><span class="keyword">template</span> &lt;&gt; <span class="keyword">struct</span> <span class="title class_">DerivedToFront</span>&lt;NullType&gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> NullType Result; &#125;;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> <span class="title class_">Tail</span>&gt; <span class="keyword">class</span> <span class="title class_">DerivedToFront</span>&lt; Typelist&lt;Head, Tail&gt; &gt;</span><br><span class="line">&#123; <span class="keyword">typedef</span> <span class="keyword">typename</span> MostDerived&lt;Tail, Head&gt;::Result TheMostDerived;</span><br><span class="line">  <span class="keyword">typedef</span> <span class="keyword">typename</span> Replace&lt;Tail, TheMostDerived, Head&gt;::Result L;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="keyword">typedef</span> Typelist&lt;TheMostDerived, L&gt; Result; &#125;</span><br></pre></td></tr></table></figure>

<h3 id="類的自動生成"><a href="#類的自動生成" class="headerlink" title="類的自動生成"></a>類的自動生成</h3><pre class="mermaid">
classDiagram
class WidgetInfo
class Holder1~Widget~
class Holder2~std::string~
class Holder3~int~
class GenScatterHierarchy1~Widget, Holder~
class GenScatterHierarchy2~NullType, Holder~
class GenScatterHierarchy3~TYPELIST_1(Widget), Holder~
class GenScatterHierarchy4~std::string, Holder~
class GenScatterHierarchy5~int, Holder~
class GenScatterHierarchy6~TYPELIST_2(std::string, Widget), Holder~
GenScatterHierarchy1 ..&gt; Holder1 : 泛化
GenScatterHierarchy4 ..&gt; Holder2 : 泛化
GenScatterHierarchy5 ..&gt; Holder3 : 泛化
GenScatterHierarchy3 ..&gt; GenScatterHierarchy2 : 泛化
GenScatterHierarchy3 ..&gt; GenScatterHierarchy1 : 泛化
GenScatterHierarchy6 ..&gt; GenScatterHierarchy4 : 泛化
GenScatterHierarchy6 ..&gt; GenScatterHierarchy3 : 泛化
WidgetInfo ..&gt; GenScatterHierarchy6 : 泛化
WidgetInfo ..&gt; GenScatterHierarchy5 : 泛化
</pre>

<details class="note info no-icon"><summary><p>GenScatterHierarchy</p>
</summary>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">TList</span>, <span class="keyword">template</span> &lt;<span class="keyword">class</span>&gt; <span class="keyword">class</span> <span class="title class_">Unit</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GenScatterHierarchy</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// GenScatterHierarchy specialization: Typelist to Unit</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T1</span>, <span class="keyword">class</span> <span class="title class_">T2</span>, <span class="keyword">template</span> &lt;<span class="keyword">class</span>&gt; <span class="keyword">class</span> <span class="title class_">Unit</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GenScatterHierarchy</span>&lt;<span class="built_in">TYPELIST_2</span>(T1, T2), Unit&gt;</span><br><span class="line">  : <span class="keyword">public</span> GenScatterHierarchy&lt;T1, Unit&gt;</span><br><span class="line">  , <span class="keyword">public</span> GenScatterHierarchy&lt;T2, Unit&gt;</span><br><span class="line">&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Pass an atomic type (nontypelist) to Unit</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">AtomicType</span>, <span class="keyword">template</span> &lt;<span class="keyword">class</span>&gt; <span class="keyword">class</span> <span class="title class_">Unit</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GenScatterHierarchy</span> : <span class="keyword">public</span> Unit&lt;AtomicType&gt;</span><br><span class="line">&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Do nothing for NullType</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">template</span> &lt;<span class="keyword">class</span>&gt; <span class="keyword">class</span> <span class="title class_">Unit</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">GenScatterHierarchy</span>&lt;NullType, Unit&gt;</span><br><span class="line">&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// How to use:</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// template &lt;class T&gt;</span></span><br><span class="line"><span class="comment">// struct Holder &#123; T value_; &#125;;</span></span><br><span class="line"><span class="comment">// </span></span><br><span class="line"><span class="comment">// typedef GenScatterHierarchy&lt;TYPELIST_3(int, std::string,  Widget), Holder&gt; WidgetInfo;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>
</details>]]></content>
      <categories>
        <category>cpp</category>
        <category>loki</category>
      </categories>
      <tags>
        <tag>loki</tag>
      </tags>
  </entry>
  <entry>
    <title>參營日常 - OSCamp2022</title>
    <url>/2022/07/06/os/camp/note/</url>
    <content><![CDATA[<p>這是 Open-Source OS Training Comp 2022 的參營日常，主要做一些訓練的筆記，以備回顧和分享。周知！嚴格意義上并不是技術文檔，可以作爲一種筆記日志。</p>
<span id="more"></span>

<h1 id="第一階段的學習記錄"><a href="#第一階段的學習記錄" class="headerlink" title="第一階段的學習記錄"></a>第一階段的學習記錄</h1><h2 id="主要的任务"><a href="#主要的任务" class="headerlink" title="主要的任务"></a>主要的任务</h2><h3 id="Step-0-Rustlings"><a href="#Step-0-Rustlings" class="headerlink" title="Step - 0: Rustlings"></a>Step - 0: Rustlings</h3><details class="note primary"><summary><p>十分重要的Rustlings</p>
</summary>
<div class="tabs" id="rustlings"><ul class="nav-tabs"><li class="tab active"><a href="#rustlings-1">问题列表</a></li><li class="tab"><a href="#rustlings-2">Rustlings 问题汇总</a></li><li class="tab"><a href="#rustlings-3">Rust的总结</a></li></ul><div class="tab-content"><div class="tab-pane active" id="rustlings-1"><p><strong>Problem List</strong></p>
<ul>
<li><input checked="" disabled="" type="checkbox"> advanced_errors [2]</li>
<li><input checked="" disabled="" type="checkbox"> clippy [2]</li>
<li><input checked="" disabled="" type="checkbox"> collections [4]</li>
<li><input checked="" disabled="" type="checkbox"> conversions [5]</li>
<li><input checked="" disabled="" type="checkbox"> enumms [3]</li>
<li><input checked="" disabled="" type="checkbox"> error_handling [6]</li>
<li><input checked="" disabled="" type="checkbox"> functions [5]</li>
<li><input checked="" disabled="" type="checkbox"> generics [3]</li>
<li><input checked="" disabled="" type="checkbox"> if [2]</li>
<li><input checked="" disabled="" type="checkbox"> intro [2]</li>
<li><input checked="" disabled="" type="checkbox"> macros [4]</li>
<li><input checked="" disabled="" type="checkbox"> modules [3]</li>
<li><input checked="" disabled="" type="checkbox"> move_semantics [6]</li>
<li><input checked="" disabled="" type="checkbox"> option [3]</li>
<li><input checked="" disabled="" type="checkbox"> primitive_types [6]</li>
<li><input checked="" disabled="" type="checkbox"> standard_library_types [7]</li>
<li><input checked="" disabled="" type="checkbox"> strings [2]</li>
<li><input checked="" disabled="" type="checkbox"> structs [3]</li>
<li><input checked="" disabled="" type="checkbox"> tests [3]</li>
<li><input checked="" disabled="" type="checkbox"> threads [1]</li>
<li><input checked="" disabled="" type="checkbox"> traits [2]</li>
<li><input checked="" disabled="" type="checkbox"> variables [6]</li>
<li><input checked="" disabled="" type="checkbox"> quiz<ul>
<li><input checked="" disabled="" type="checkbox"> 1</li>
<li><input checked="" disabled="" type="checkbox"> 2</li>
<li><input checked="" disabled="" type="checkbox"> 3</li>
<li><input checked="" disabled="" type="checkbox"> 4</li>
</ul>
</li>
</ul>
<div class="note default"><p>这是实验需要完成的任务笔记</p>
</div></div><div class="tab-pane" id="rustlings-2"><div class="tabs" id="rslq"><ul class="nav-tabs"><li class="tab active"><a href="#rslq-1">Rust的生命周期</a></li><li class="tab"><a href="#rslq-2">Rust异常处理</a></li><li class="tab"><a href="#rslq-3">rslQ 3</a></li></ul><div class="tab-content"><div class="tab-pane active" id="rslq-1"><div class="note primary"><p>这一部分主要聊一下， rust中的生命周期</p>
</div>

<figure class="highlight rust"><figcaption><span>RustStatic</span><a href="https://github.com/LearningOS/learn_rust_rustlings-Andre-HJR/blob/03c8cb301d718bc0e6f8798ebc2ced8924bb89e7/exercises/strings/strings1.rs#L1">string2.rs</a></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">answer</span> = <span class="title function_ invoke__">current_favorite_color</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;My current favorite color is &#123;&#125;&quot;</span>, answer);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">current_favorite_color</span>() <span class="punctuation">-&gt;</span> &amp;<span class="symbol">&#x27;static</span> <span class="type">str</span> &#123;</span><br><span class="line">    <span class="string">&quot;blue&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong><code>&amp;&#39;static</code></strong></p>
<p><code>&amp;&#39;static</code>对于生命周期有着非常强的要求：一个引用必须要活得跟剩下得程序一样就，才能被标注为<code>&amp;&#39;static</code>。</p>
<p>对于字符串字面量来说，它直接被打包到二进制文件中，永远不会被<code>drop</code>，因此它能跟程序活得一样久，自然它得生命周期是<code>&#39;static</code>。</p>
<p>但是, <strong><code>&amp;&#39;static</code>生命周期针对得仅仅是引用，而不是持有该应用得变量，对于变量来说，还是要遵循相应得作用域规则</strong>。</p>
<figure class="highlight rust"><figcaption><span>CodeDemo</span></figcaption><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">use</span> std::&#123;slice::from_raw_parts, str::from_utf8_unchecked&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">get_memory_location</span>() <span class="punctuation">-&gt;</span> (<span class="type">usize</span>, <span class="type">usize</span>) &#123;</span><br><span class="line">  <span class="comment">// “Hello World” 是字符串字面量，因此它的生命周期是 `&#x27;static`.</span></span><br><span class="line">  <span class="comment">// 但持有它的变量 `string` 的生命周期就不一样了，它完全取决于变量作用域，对于该例子来说，也就是当前的函数范围</span></span><br><span class="line">  <span class="keyword">let</span> <span class="variable">string</span> = <span class="string">&quot;Hello World!&quot;</span>;</span><br><span class="line">  <span class="keyword">let</span> <span class="variable">pointer</span> = string.<span class="title function_ invoke__">as_ptr</span>() <span class="keyword">as</span> <span class="type">usize</span>;</span><br><span class="line">  <span class="keyword">let</span> <span class="variable">length</span> = string.<span class="title function_ invoke__">len</span>();</span><br><span class="line">  (pointer, length)</span><br><span class="line">  <span class="comment">// `string` 在这里被 drop 释放</span></span><br><span class="line">  <span class="comment">// 虽然变量被释放，无法再被访问，但是数据依然还会继续存活</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">get_str_at_location</span>(pointer: <span class="type">usize</span>, length: <span class="type">usize</span>) <span class="punctuation">-&gt;</span> &amp;<span class="symbol">&#x27;static</span> <span class="type">str</span> &#123;</span><br><span class="line">  <span class="comment">// 使用裸指针需要 `unsafe&#123;&#125;` 语句块</span></span><br><span class="line">  <span class="keyword">unsafe</span> &#123; <span class="title function_ invoke__">from_utf8_unchecked</span>(<span class="title function_ invoke__">from_raw_parts</span>(pointer <span class="keyword">as</span> *<span class="keyword">const</span> <span class="type">u8</span>, length)) &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">  <span class="keyword">let</span> (pointer, length) = <span class="title function_ invoke__">get_memory_location</span>();</span><br><span class="line">  <span class="keyword">let</span> <span class="variable">message</span> = <span class="title function_ invoke__">get_str_at_location</span>(pointer, length);</span><br><span class="line">  <span class="built_in">println!</span>(</span><br><span class="line">    <span class="string">&quot;The &#123;&#125; bytes at 0x&#123;:X&#125; stored: &#123;&#125;&quot;</span>,</span><br><span class="line">    length, pointer, message</span><br><span class="line">  );</span><br><span class="line">  <span class="comment">// 如果大家想知道为何处理裸指针需要 `unsafe`，可以试着反注释以下代码</span></span><br><span class="line">  <span class="comment">// let message = get_str_at_location(1000, 10);</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>上面代码有两点值得注意：</p>
<ul>
<li><code>&amp;&#39;static</code> 的引用确实可以和程序活得一样久，因为我们通过 <code>get_str_at_location</code> 函数直接取到了对应的字符串</li>
<li>持有 <code>&amp;&#39;static</code> 引用的变量，它的生命周期受到作用域的限制，大家务必不要搞混了</li>
</ul>
<p><strong><code>T: &#39;static</code></strong></p>
<p>相比起来，这种形式的约束就有些复杂了。</p>
<p>首先，在以下两种情况下，<code>T: &#39;static</code> 与 <code>&amp;&#39;static</code> 有相同的约束：<code>T</code> 必须活得和程序一样久。</p>
<figure class="highlight rust"><figcaption><span>CodeDemo</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::fmt::<span class="built_in">Debug</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">print_it</span>&lt;T: <span class="built_in">Debug</span> + <span class="symbol">&#x27;static</span>&gt;( input: T) &#123;</span><br><span class="line">    <span class="built_in">println!</span>( <span class="string">&quot;&#x27;static value passed in is: &#123;:?&#125;&quot;</span>, input );</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">print_it1</span>( input: <span class="keyword">impl</span> <span class="title class_">Debug</span> + <span class="symbol">&#x27;static</span> ) &#123;</span><br><span class="line">    <span class="built_in">println!</span>( <span class="string">&quot;&#x27;static value passed in is: &#123;:?&#125;&quot;</span>, input );</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">i</span> = <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">    <span class="title function_ invoke__">print_it</span>(&amp;i);</span><br><span class="line">    <span class="title function_ invoke__">print_it1</span>(&amp;i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<p>以上代码会报错，原因很简单: <code>&amp;i</code> 的生命周期无法满足 <code>&#39;static</code> 的约束，如果大家将 <code>i</code> 修改为常量，那自然一切 <code>OK</code>。</p>
<p>见证奇迹的时候，请不要眨眼，现在我们来稍微修改下 <code>print_it</code> 函数:</p>
<figure class="highlight rust"><figcaption><span>CodeDemo</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::fmt::<span class="built_in">Debug</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">print_it</span>&lt;T: <span class="built_in">Debug</span> + <span class="symbol">&#x27;static</span>&gt;( input: &amp;T) &#123;</span><br><span class="line">    <span class="built_in">println!</span>( <span class="string">&quot;&#x27;static value passed in is: &#123;:?&#125;&quot;</span>, input );</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">i</span> = <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">    <span class="title function_ invoke__">print_it</span>(&amp;i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码竟然不报错了！原因在于我们约束的是 <code>T</code>，但是使用的却是它的引用 <code>&amp;T</code>，换而言之，我们根本没有直接使用 <code>T</code>，因此编译器就没有去检查 <code>T</code> 的生命周期约束！它只要确保 <code>&amp;T</code> 的生命周期符合规则即可，在上面代码中，它自然是符合的。</p>
<div class="note info no-icon"><p><strong>需要注意的是</strong>: <code>&amp;&#39; static</code>这个引用是针对指向数据，而引用本身是要遵循其作用域范围的。（有指针那味道了）</p>
</div>

<p><strong>简单总结一下</strong></p>
<p>总之， <code>&amp;&#39;static</code> 和 <code>T: &#39;static</code> 大体上相似，相比起来，后者的使用形式会更加复杂一些。</p>
<p>至此，相信大家对于 <code>&#39;static</code> 和 <code>T: &#39;static</code> 也有了清晰的理解，那么我们应该如何使用它们呢？</p>
<p>作为经验之谈，可以这么来:</p>
<ul>
<li>如果你需要添加 <code>&amp;&#39;static</code> 来让代码工作，那很可能是设计上出问题了</li>
<li>如果你希望满足和取悦编译器，那就使用 <code>T: &#39;static</code>，很多时候它都能解决问题</li>
</ul></div><div class="tab-pane" id="rslq-2"><details class="note info no-icon"><summary><p>作业中的ErrorHandler</p>
</summary>
<p><strong>这是一些例题</strong></p>
<details class="note primary no-icon"><summary><p>ErrorHandler</p>
</summary>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">total_cost</span>(item_quantity: &amp;<span class="type">str</span>) =&gt; <span class="type">Result</span>&lt;<span class="type">i32</span>, ParseIntError&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">processing_fee</span> = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">cost_per_item</span>  = <span class="number">5</span>;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">qty</span> = item_quantity.parse::&lt;<span class="type">i32</span>&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(qty * cost_per_item + processing_fee)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">total_cost</span>(item_quantity: &amp;<span class="type">str</span>) =&gt; <span class="type">Result</span>&lt;<span class="type">i32</span>, ParseIntError&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">processing_fee</span> = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">cost_per_item</span>  = <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">qty</span> = item_quantity.parse::&lt;<span class="type">i32</span>&gt;()?;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(qty * cost_per_item + processing_fee)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</details>

<details class="note primary no-icon"><summary><p>AdvanceErrorHandler</p>
</summary>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">from</span>(e: CreationError) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123;</span><br><span class="line">    <span class="comment">// <span class="doctag">TODO:</span> complete this implementation so that the `?` operator will</span></span><br><span class="line">    <span class="comment">// work for `CreationError`</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// <span class="doctag">TODO:</span> implement another instance of the `From` trait here so that the</span></span><br><span class="line"><span class="comment">// `?` operator will work in the other place in the `FromStr`</span></span><br><span class="line"><span class="comment">// implementation below</span></span><br></pre></td></tr></table></figure>

<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">from</span>(e: CreationError) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123;</span><br><span class="line">    <span class="comment">// <span class="doctag">TODO:</span> complete this implementation so that the `?` operator will</span></span><br><span class="line">    <span class="comment">// work for `CreationError`</span></span><br><span class="line">    <span class="keyword">Self</span>::<span class="title function_ invoke__">Creation</span>(e)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// <span class="doctag">TODO:</span> implement another instance of the `From` trait here so that the</span></span><br><span class="line"><span class="comment">// `?` operator will work in the other place in the `FromStr`</span></span><br><span class="line"><span class="comment">// implementation below</span></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">From</span>&lt;ParseIntError&gt; <span class="keyword">for</span> <span class="title class_">ParsePosNonzeroError</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">from</span>(e: ParseIntError) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123; <span class="keyword">Self</span>::<span class="title function_ invoke__">ParseInt</span>(e) &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</details>

</details>

<p><strong>Rust的错误哲学</strong></p>
<p>错误对于软件来说是不可能避免的，因此一门优秀的编程语言必须有其完整的错误处理哲学。在很多情况下，Rust需要你承认自己的 代码可能会出错，并提前采取行动，来处理这些错误。</p>
<details class="note default no-icon"><summary><p>Rust中的错误主要分为两类</p>
</summary>
<ul>
<li><strong>可恢复错误</strong>，通常用于从系统全局角度来看可以接受的错误，例如处理用户的访问、操作等错误，这些错误只会影响某个用户自身的操作进程，而不会对系统的全局稳定性产生影响。</li>
<li><strong>不可恢复错误</strong>，刚好相反，该错误通常是全局性或者系统性的错误，例如数组越界访问，系统启动时发生了影响启动流程的错误等等，这些错误的影响往往对于系统来说是致命的。</li>
</ul>

</details>

<details class="note danger no-icon"><summary><p>Attention</p>
</summary>
<p>很多编程语言，并不会区分这些错误，而是直接采用异常的方式去处理。Rust没有异常，但是Rust也有自己的卧龙凤雏： <code>Result&lt;T, E&gt;</code>用于可恢复错误，<code>panic!</code>用于不可恢复错误。</p>

</details>

<p><strong><code>PANIC</code>部分</strong></p>
<details class="note info no-icon"><summary><p><code>panic</code>时的两种终止方式</p>
</summary>
<p>当出现<code>panic!</code>时，程序提供了两种方式来处理终止流程：<strong>栈展开</strong>和<strong>直接终止</strong></p>
<p>其中，默认的方式就是 栈展开，这意味着<code>Rust</code>会回溯栈上数据和函数调用，因此也意味着更多的善后工作，好处是可以给出充分的报错信息和栈调用信息，便于事后的问题复盘。直接终止，顾名思义，不清理数据就直接退出程序，善后工作交与操作系统来负责。</p>
<p>对于绝大多数用户，使用默认选择是最好的，但是当你关心最终编译出的二进制可执行文件大小时，那么可以尝试去使用直接终止的方式，例如下面的配置修改<code>Cargo.toml</code>文件，实现在<code>release</code>模式下遇到<code>panic</code>直接终止：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[profile.release]</span></span><br><span class="line"><span class="attr">panic</span> = <span class="string">&#x27;abort&#x27;</span></span><br></pre></td></tr></table></figure>

</details>

<details class="note danger no-icon"><summary><p>何时该<code>panic!</code></p>
</summary>
<p>下面尝试罗列何时适合使用<code>panic</code>，首先我们先看一些背景</p>
<details class="note info no-icon"><summary><p>前景知识</p>
</summary>
<p>在<code>Result&lt;T, E&gt;</code>这个枚举类型，它是用来表示函数的返回结果：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">Result</span>&lt;T, E&gt; &#123;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(T),</span><br><span class="line">    <span class="title function_ invoke__">Err</span>(E),</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当没有错误发生时，函数返回一个用<code>Result</code>类型包裹的值<code>Ok(T)</code>，当错误时，返回一个<code>Err(E)</code>。对于<code>Result</code>返回我们有很多处理方法，最见到那粗暴的解释<code>unwrap</code>和<code>except</code>，这两个函数非常类似，我们以<code>unwrap</code>举例：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::net::IpAddr;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">lo</span>: IpAddr = <span class="string">&quot;127.0.0.1&quot;</span>.<span class="title function_ invoke__">parse</span>().<span class="title function_ invoke__">unwrap</span>();</span><br></pre></td></tr></table></figure>

<p>上面<code>parse</code>方法试图将字符串<code>&quot;127.0.0.1&quot;</code>解析位一个<code>IP</code>地址类型<code>IpAddr</code>，它返回一个<code>Result&lt;IpAddr, E&gt;</code>类型，如果解析成功，则把<code>Ok(IpAddr)</code>中的值赋值给<code>lo</code>，如果失败，则不处理<code>Err(E)</code>，而是直接<code>panic</code></p>
<p>因此 <code>unwrap</code> 简言之，成功就是返回值，失败则是<code>panic</code>，总之并不能进行错误处理。</p>

</details>

<p><strong>示例、原型、测试</strong></p>
<p>这几个场景下，需要快速地搭建代码，错误处理会拖慢编码的速度，也不是特别有必要，因此通过<code>unwrap</code>、<code>expect</code>等方法来处理是最快的。</p>
<p>同时，当我们回头准备做错误处理时，可以全局搜索这些方法，不遗漏地进行替换。</p>
<p><strong>确切纸雕程序是正确的，可以使用<code>panic</code></strong></p>
<p>因为<code>panic</code>的触发方式比错误处理要简单，因此可以让代码更清晰，可读性也更加好，当我们的代码注定是正确时，你可以用<code>unwrap</code>等方法直接进行处理，反正也不可能<code>panic</code>：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::net::IpAddr;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">lo</span>: IpAddr = <span class="string">&quot;127.0.0.1&quot;</span>.<span class="title function_ invoke__">parse</span>().<span class="title function_ invoke__">unwrap</span>();</span><br></pre></td></tr></table></figure>
例如上面的例子，`"127.0.0.1"`就是`ip`地址，因此我们知道`parse`方法一定会成功，那么就可以直接用`unwrap`方法进行处理。

<p>当然，如果该字符串是来自于用户输入，那在实际项目中，就必须用错误处理的方式，而不是<code>unwrap</code>，否则你的程序一天要崩溃几十万次吧！</p>
<p><strong>可能导致全局有害状态时</strong></p>
<details class="note danger no-icon"><summary><p>有害状态分类</p>
</summary>
<ul>
<li>非预期的错误</li>
<li>后续代码的运行会受到显著影响</li>
<li>内存安全的问题</li>
</ul>

</details>

<p>当错误预期会出现时，返回一个错误较为合适，例如解析器接收到格式错误的数据，<code>HTTP</code>请求接收到错误的参数甚至该请求内的任何错误（不会导致整个程序有问题，只影响该次请求）。因为错误是可预期的，因此也是可以处理的。</p>
<p>当启动时某个流程发生了错误，对后续代码的运行造成了影响，那么就应该使用<code>panic</code>，而不是处理错误后继续运行，当然你可以通过重试的方式来继续。</p>
<p>上面提到过，数组访问越界，就要<code>panic</code>的原因，这个就是属于内存安全的范畴，一旦内存访问不安全，那么我们就无法保证自己的程序会怎么运行下去，也无法保证逻辑和数据的正确性。</p>
<details class="note default no-icon"><summary><p><code>panic</code>原理剖析</p>
</summary>
<p>本来不想写这块儿内容，因为真的难写，但是转念一想，既然号称圣经，那么本书就得与众不同，避重就轻显然不是该有的态度。</p>
<p>当调用<code>panic!</code>宏时，它会</p>
<details class="note danger no-icon"><summary><p><code>panic!</code>的流程</p>
</summary>
<ol>
<li>格式化<code>panic</code>信息，然后使用该信息作为参数，调用<code>std::panic::panic_any()</code>函数</li>
<li><code>panic_any</code>会检查应用是否使用了<code>panic hook</code>，如果使用了，该<code>hook</code>函数就会被调用（<code>hook</code>是一个钩子函数，是外部代码设置的，用于在<code>panic</code>触发时，执行外部代码所需的功能）</li>
<li>当<code>hook</code>函数返回后，当前的线程就开始进行栈展开：从<code>panic_any</code>开始，如果寄存器或者栈因为某些原因信息错乱了，那很可能该展开会发生异常，最终线程会直接停止，展开也无法继续进行</li>
<li>展开的过程是一帧一帧的去回溯整个栈，每个帧的数据都会随之被丢弃，但是在展开过程中，你可能会遇到被用户标记为<code>catching</code>的帧（通过<code>std::panic::catch_unwind()</code>函数标记），此时用户提供的<code>catch</code>函数会被调用，展开也随之停止：当然，如果<code>catch</code>选择在内部调用<code>std::panic::resume_unwind()</code>函数，则展开还会继续。</li>
</ol>

</details>

<p>还有一种情况，在展开过程中，如果展开本身<code>panic</code>了，那展开线程会终止，展开也随之停止。</p>
<p>一旦线程展开被终止或者完成，最终的输出结果是取决于哪个线程<code>panic</code>：对于<code>main</code>线程，操作系统提供的终止功能<code>core::intrinsics::abort()</code>会被调用，最终结束当前的<code>panic</code>进程；如果是其它子线程，那么子线程就会简单的终止，同时信息会在稍后通过<code>std::thread::join()</code>进行收集。</p>

</details>


</details>

<p><strong>可恢复的错误<code>Result</code></strong></p>
<p>假设，我们有一台消息服务器，每个用户都通过<code>websocket</code>连接到该服务器来接收和发送消息，该过程就涉及到<code>socket</code>文件的读写，那么此时，如果一个用户的读写发生了错误，显然不能直接<code>panic</code>，否则服务器会直接崩溃，所有用户都会断开连接，因此我们需要一种更温和的错误处理方式：<code>Result&lt;T, E&gt;</code>。</p>
<p><code>Result</code>的定义如下:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">Result</span>&lt;T, E&gt; &#123;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(T), </span><br><span class="line">    <span class="title function_ invoke__">Err</span>(E),</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>泛型参数<code>T</code>代表成功时存入的正确值的类型，存放方式是<code>Ok(T)</code>，<code>E</code>代表错误是存入的错误值，存放方式是<code>Err(E)</code>。</p>
<p>由于<code>Result</code>是被包含在<code>prelude</code>中，因此无需手动引入<code>std::result::Result</code>，那么返回值可以简化位<code>Result&lt;std::fs::File, std::io::Error&gt;</code>，这很像标准的<code>Result&lt;T, E&gt;</code>枚举定义，只不过<code>T,E</code>被替换成了相应具体的类型。</p>
<p>这个返回值类型说明<code>File::open</code>调用成功则返回一个可以进行读写的文件句柄，如果失败，则返回一个IO错误。总之<code>File::open</code>需要一个方式告知调用者是成功还是失败，并同时返回具体的文件句柄或错误信息，我们可以通过<code>Result</code>的枚举提供，如下：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::fs::File;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">f</span> = File::<span class="title function_ invoke__">open</span>(<span class="string">&quot;Hello.txt&quot;</span>);</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">f</span> = <span class="keyword">match</span> f &#123;</span><br><span class="line">        <span class="title function_ invoke__">Ok</span>(file) =&gt; file,</span><br><span class="line">        <span class="title function_ invoke__">Err</span>(error) =&gt; &#123;</span><br><span class="line">            <span class="built_in">panic!</span>(<span class="string">&quot;Problem opening the file: &#123;:?&#125;&quot;</span>, error)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>代码很清晰，对打开文件后的<code>Result&lt;T, E&gt;</code>类型进行匹配取值，如果是成功，则将<code>Ok(file)</code>中存放的的文件句柄<code>file</code>赋值给<code>f</code>，如果失败，则将<code>Err(error)</code>中存放的错误信息<code>error</code>使用<code>panic</code>抛出来，进而结束程序，这非常符合上文提到过的<code>panic</code>使用场景。</p>
<p><strong>错误传播</strong></p>
<p>咱们的程序几乎不太可能只有<code>A-&gt;B</code>形式的函数调用，一个设计良好的程序，一个功能涉及十几层的函数调用都有可能。而错误处理也往往不是哪里调用出错，就在哪里处理，实际应用中，大概率会把错误层层上传然后交给调用链的上游函数进行处理，错误传播将极为常见。</p>
<p>例如以下函数从文件中读取用户名，然后将结果进行返回：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">#![allow(unused)]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line"><span class="keyword">use</span> std::fs::File;</span><br><span class="line"><span class="keyword">use</span> std::io::&#123;<span class="keyword">self</span>, Read&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">read_username_from_file</span>() <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;<span class="type">String</span>, io::Error&gt; &#123;</span><br><span class="line">    <span class="comment">// 打开文件，f是`Result&lt;文件句柄,io::Error&gt;`</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">f</span> = File::<span class="title function_ invoke__">open</span>(<span class="string">&quot;hello.txt&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">f</span> = <span class="keyword">match</span> f &#123;</span><br><span class="line">        <span class="comment">// 打开文件成功，将file句柄赋值给f</span></span><br><span class="line">        <span class="title function_ invoke__">Ok</span>(file) =&gt; file,</span><br><span class="line">        <span class="comment">// 打开文件失败，将错误返回(向上传播)</span></span><br><span class="line">        <span class="title function_ invoke__">Err</span>(e) =&gt; <span class="keyword">return</span> <span class="title function_ invoke__">Err</span>(e),</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="comment">// 创建动态字符串s</span></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">s</span> = String::<span class="title function_ invoke__">new</span>();</span><br><span class="line">    <span class="comment">// 从f文件句柄读取数据并写入s中</span></span><br><span class="line">    <span class="keyword">match</span> f.<span class="title function_ invoke__">read_to_string</span>(&amp;<span class="keyword">mut</span> s) &#123;</span><br><span class="line">        <span class="comment">// 读取成功，返回Ok封装的字符串</span></span><br><span class="line">        <span class="title function_ invoke__">Ok</span>(_) =&gt; <span class="title function_ invoke__">Ok</span>(s),</span><br><span class="line">        <span class="comment">// 将错误向上传播</span></span><br><span class="line">        <span class="title function_ invoke__">Err</span>(e) =&gt; <span class="title function_ invoke__">Err</span>(e),</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>有几点值得注意：</p>
<ul>
<li>该函数返回一个<code>Result&lt;String, io::Error&gt;</code>类型，当读取用户名成功时，返回<code>Ok(String)</code>，失败时，返回<code>Err(io:Error)</code></li>
<li><code>File::open</code>和<code>f.read_to_string</code>返回的<code>Result&lt;T, E&gt;</code>中的<code>E</code>就是<code>io::Error</code></li>
</ul>
<p>由此可见，该函数将<code>io::Error</code>的错误往上进行传播，该函数的调用者最终会对<code>Result&lt;String,io::Error&gt;</code>进行再处理，至于怎么处理就是调用者的事，如果是错误，它可以选择继续向上传播错误，也可以直接<code>panic</code>，亦或将具体的错误原因包装后写入<code>socket</code>中呈现给终端用户。</p>
<p>但是上面的代码也有自己的问题，那就是太长了(优秀的程序员身上的优点极多，其中最大的优点就是懒)，我自认为也有那么一点点优秀，因此见不到这么啰嗦的代码，下面咱们来讲讲如何简化它。</p>
<details class="note default no-icon"><summary><p><code>?</code>特殊的传播符号</p>
</summary>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::fs::File;</span><br><span class="line"><span class="keyword">use</span> std::io;</span><br><span class="line"><span class="keyword">use</span> std::io::Read;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">read_username_from_file</span>() <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;<span class="type">String</span>, io::Error&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">f</span> = File::<span class="title function_ invoke__">open</span>(<span class="string">&quot;hello.txt&quot;</span>)?;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">s</span> = String::<span class="title function_ invoke__">new</span>();</span><br><span class="line">    f.<span class="title function_ invoke__">read_to_string</span>(&amp;<span class="keyword">mut</span> s)?;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(s)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
看到没，这就是排面，相比前面的`match`处理错误的函数，代码直接减少了一半不止，但是，一山更比一山难，看不懂啊！ 其实`?`就是一个宏，它的作用跟上面的`match`几乎一模一样。


<p>虽然<code>?</code>和<code>match</code>功能一致，但是事实上<code>?</code>会更胜一筹。何解？</p>
<p>想象一下，一个设计良好的系统中，肯定有自定义的错误特征，错误之间很可能会存在上下级关系，例如标准库中的<code>std::io::Error</code>和<code>std::error::Error</code>，前者是<code>IO</code>相关的错误结构体，后者是一个最最通用的标准错误特征，同时前者实现了后者，因此<code>std::io::Error</code>可以转换为<code>std:error::Error</code>。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">open_file</span>() <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;File, <span class="type">Box</span>&lt;<span class="keyword">dyn</span> std::error::Error&gt;&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">f</span> = File::<span class="title function_ invoke__">open</span>(<span class="string">&quot;hello.txt&quot;</span>)?;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(f)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中<code>File::open</code>报错时返回的错误是<code>std::io::Error</code>类型，但是<code>open_file</code>函数返回的错误类型是<code>std::error::Error</code>的特征对象，可以看到一个错误类型通过<code>?</code>返回后，变成了另一个错误类型，这就是<code>?</code>的神奇之处。</p>
<p>根本原因是在于标准库中定义的<code>From</code>特征，该特征有一个方法<code>from</code>，用于把一个类型转成另外一个类型，<code>?</code>可以自动调用该方法，然后进行隐式类型转换。因此只要函数返回的错误<code>ReturnError</code>实现了<code>From&lt;OtherError&gt;</code>特征，那么<code>?</code>就会自动把<code>OtherError</code>转换为<code>ReturnError</code>。</p>
<p>这种转换非常好用，意味着你可以用一个大而全的<code>ReturnError</code>来覆盖所有错误类型，只需要为各种子错误类型实现这种转换即可。</p>
<p><code>?</code>还能实现链式调用，<code>File::open</code>遇到错误就返回，没有错误就将<code>Ok</code>中的值取出来用于下一个方法调用，简直太精妙了。</p>
<details class="note info no-icon"><summary><p><code>?</code>用于<code>Option</code>返回</p>
</summary>
<p><code>?</code>不仅仅可以用于<code>Result</code>的传播，还能用于<code>Option</code>的传播，再来回忆下<code>Option</code>的定义：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">enum</span> <span class="title class_">Option</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="title function_ invoke__">Some</span>(T),</span><br><span class="line">    <span class="literal">None</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
`Result`通过`?`返回错误，那么`Option`就通过`?`返回`None`：

<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">first</span>(arr: &amp;[<span class="type">i32</span>]) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;&amp;<span class="type">i32</span>&gt; &#123;</span><br><span class="line">   <span class="keyword">let</span> <span class="variable">v</span> = arr.<span class="title function_ invoke__">get</span>(<span class="number">0</span>)?;</span><br><span class="line">   <span class="title function_ invoke__">Some</span>(v)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
上面的函数中，`arr.get`返回一个`Option<&i32>`类型，因为`?`的使用，如果`get`的结果是`None`，则直接返回`None`，如果是`Some(&i32)`，则把里面的值赋给`v`。

<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">last_char_of_first_line</span>(text: &amp;<span class="type">str</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;<span class="type">char</span>&gt; &#123;</span><br><span class="line">    text.<span class="title function_ invoke__">lines</span>().<span class="title function_ invoke__">next</span>()?.<span class="title function_ invoke__">chars</span>().<span class="title function_ invoke__">last</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码展示了在链式调用中使用<code>?</code>提前返回<code>None</code>的用法，<code>.next</code>方法返回的是<code>Option</code>类型：如果返回<code>Some(&amp;str)</code>，那么继续调用<code>chars</code>方法,如果返回<code>None</code>，则直接从整个函数中返回<code>None</code>，不再继续进行链式调用。</p>

</details>

<details class="note danger no-icon"><summary><p><code>?</code>常会犯的错</p>
</summary>
<p>初学者在用<code>?</code>时，老是会犯错，例如写出这样的代码：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">first</span>(arr: &amp;[<span class="type">i32</span>]) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;&amp;<span class="type">i32</span>&gt; &#123;</span><br><span class="line">   arr.<span class="title function_ invoke__">get</span>(<span class="number">0</span>)?</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码无法通过编译，切记：<code>?</code>操作符需要一个变量来承载正确的值，这个函数只会返回<code>Some(&amp;i32)</code>或者<code>None</code>，只有错误值能直接返回，正确的值不行，所以如果数组中存在<code>0</code>号元素，那么函数第二行使用<code>?</code>后的返回类型为<code>&amp;i32</code>而不是<code>Some(&amp;i32)</code>。因此 ? 只能用于以下形式：</p>
<ul>
<li><code>let v = xxx()?</code>;</li>
<li><code>xxx()?.yyy()?</code>;</li>
</ul>
<p><strong>带返回值的<code>main</code>函数</strong></p>
<p>在了解了<code>?</code>的使用限制后，这段代码你很容易看出它无法编译：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::fs::File;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">f</span> = File::<span class="title function_ invoke__">open</span>(<span class="string">&quot;hello.txt&quot;</span>)?;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
因为`?`要求`Result<T, E>`形式的返回值，而`main`函数的返回是`()`，因此无法满足，那是不是就无解了呢？

<p>实际上<code>Rust</code>还支持另外一种形式的<code>main</code>函数：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::error::Error;</span><br><span class="line"><span class="keyword">use</span> std::fs::File;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;(), <span class="type">Box</span>&lt;<span class="keyword">dyn</span> Error&gt;&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">f</span> = File::<span class="title function_ invoke__">open</span>(<span class="string">&quot;hello.txt&quot;</span>)?;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
这样就能使用`?`提前返回了，同时我们又一次看到了`Box<dyn Error>`特征对象，因为`std::error:Error`是`Rust`中抽象层次最高的错误，其它标准库中的错误都实现了该特征，因此我们可以用该特征对象代表一切错误，就算`main`函数中调用任何标准库函数发生错误，都可以通过`Box<dyn Error>`这个特征对象进行返回.

</details>

</details></div><div class="tab-pane" id="rslq-3"><p><strong>This is Sub Tab 3.</strong></p>
<div class="tabs" id="sub-sub-tabs"><ul class="nav-tabs"><li class="tab active"><a href="#sub-sub-tabs-1">Sub-Sub Tabs 1</a></li><li class="tab"><a href="#sub-sub-tabs-2">Sub-Sub Tabs 2</a></li><li class="tab"><a href="#sub-sub-tabs-3">Sub-Sub Tabs 3</a></li></ul><div class="tab-content"><div class="tab-pane active" id="sub-sub-tabs-1"><p><strong>This is Sub-Sub Tab 1 of Sub Tab 3.</strong></p></div><div class="tab-pane" id="sub-sub-tabs-2"><p><strong>This is Sub-Sub Tab 2 of Sub Tab 3.</strong></p></div><div class="tab-pane" id="sub-sub-tabs-3"><p><strong>This is Sub-Sub Tab 3 of Sub Tab 3.</strong></p></div></div></div></div></div></div></div><div class="tab-pane" id="rustlings-3"><p><strong>Rust</strong>，我觉得还是对 <strong>C++<strong>语言地位的一种挑战，本质上还是一种 <strong>C++<strong>的<code>--</code>操作。其实C++中的</strong>Move</strong>也是可以完成相应的权限的思想的。但是</strong>Rust</strong>的设计实在是太巧妙了，它将内存操作的粒度变小了，引入所有权的概念，减少<strong>C++<strong>在设计大型项目时，处理内存的</strong>Allocator</strong>，我觉得<strong>Allocator</strong>是面向对象的遗留，而事实不止一次向程序员强调面向对象是不太合理的，至少在内存处理这个方向。记得<code>STLport</code>提供了一个近乎完美的<code>Allocator Pool</code>但是，即使是那样，内存的碎片化仍然是无法避免的。而所有权的引入使得内存操作可以拥有更细的粒度，但又不会让人觉得像处理指针那样复杂，我觉得这是编程语言的一个很大的进步。</p></div></div></div>

</details>


<h3 id="Step-2-Labs"><a href="#Step-2-Labs" class="headerlink" title="Step - 2: Labs"></a>Step - 2: Labs</h3><details class="note primary"><summary><p>Lab主要的實驗報告</p>
</summary>
<p>时间还是紧张了些，Lab的实验报告，并没有全部完成，当自己的工作逐渐稳定后会补齐相应的实验记录。</p>
<div class="tabs" id="lab"><ul class="nav-tabs"><li class="tab active"><a href="#lab-1">Lab0-0</a></li><li class="tab"><a href="#lab-2">Lab0-1</a></li><li class="tab"><a href="#lab-3">Lab1</a></li><li class="tab"><a href="#lab-4">Lab2</a></li><li class="tab"><a href="#lab-5">Lab3</a></li><li class="tab"><a href="#lab-6">Lab4</a></li><li class="tab"><a href="#lab-7">Lab5</a></li></ul><div class="tab-content"><div class="tab-pane active" id="lab-1"><div class="tabs" id="lab00"><ul class="nav-tabs"><li class="tab active"><a href="#lab00-1">实验描述</a></li><li class="tab"><a href="#lab00-2">实验问题</a></li><li class="tab"><a href="#lab00-3">实验报告</a></li><li class="tab"><a href="#lab00-4">实验体会</a></li></ul><div class="tab-content"><div class="tab-pane active" id="lab00-1"><p><strong>从一个简单的HelloWorld开始</strong></p>
<p>让 Hello, world! 程序脱离其依赖的执行环境， 编写一个能打印 Hello, world! 的 OS。这趟旅途将让我们对应用程序及其执行环境有更深入的理解。</p>
<p>为便于测试，完成编程实验时，请以框架代码为基础，不必跟着文档从零开始编写内核。</p>
<p>为了做到这一步，首先需要让程序不依赖于标准库， 并通过编译。</p>
<p>接下来要让脱离了标准库的程序能输出（即支持<code>println!</code>)，这对程序的开发和调试至关重要。 我们先在用户态下实现该功能，在 此处 获取相关代码。</p>
<p>最后把程序移植到内核态，构建在裸机上支持输出的最小运行时环境。</p>
<div class="note default no-icon"><p>主要就是熟悉一下实验操作的流程和实验环境，并没有代码问题需要解决。</p>
</div></div><div class="tab-pane" id="lab00-2"><div class="note warning no-icon"><p>其实，经过这个实验，可以明显感觉到网络带来的问题，那是相当地严重。主要表现在<code>Docker</code>的系统镜像构建的时候，Ubuntu系统镜像的更新，软件安装和rust的相关安装。</p>
</div>

<details class="note info no-icon"><summary><p>Nightly的Rust</p>
</summary>
<p>Nightly 版本的编译器允许我们在源码的开头插入特性标签（feature flag），来自由选择并使用大量实验性的功能。举个例子，要使用实验性的内联汇编（<code>asm!</code>宏），我们可以在<code>main.rs</code>的顶部添加<code>#![feature(asm)]</code>。要注意的是，这样的实验性功能不稳定（unstable），意味着未来的<code>Rust</code>版本可能会修改或移除这些功能，而不会有预先的警告过渡。因此我们只有在绝对必要的时候，才应该使用这些特性。</p>

</details>

<details class="note info no-icon"><summary><p>BIOS启动梳理</p>
</summary>
<p>几乎所有的<code>x86</code>硬件系统都支持<code>BIOS</code>启动，这也包含新型的、基于<code>UEFI</code>、用模拟<code>BIOS</code>（emulated BIOS）的方式向后兼容的硬件系统。这可以说是一件好事情，因为无论是上世纪还是现在的硬件系统，你都只需编写同样的引导启动逻辑；但这种兼容性有时也是<code>BIOS</code>引导启动最大的缺点，因为这意味着在系统启动前，你的<code>CPU</code>必须先进入一个<code>16</code>位系统兼容的实模式（real mode），这样<code>1980</code>年代古老的引导固件才能够继续使用。</p>
<p>让我们从头开始，理解一遍<code>BIOS</code>启动的过程。</p>
<p>当电脑启动时，主板上特殊的闪存中存储的<code>BIOS</code>固件将被加载。<code>BIOS</code>固件将会加电自检、初始化硬件，然后它将寻找一个可引导的存储介质。如果找到了，那电脑的控制权将被转交给引导程序（bootloader）：一段存储在存储介质的开头的、512字节长度的程序片段。大多数的引导程序长度都大于512字节——所以通常情况下，引导程序都被切分为一段优先启动、长度不超过512字节、存储在介质开头的第一阶段引导程序（first stage bootloader），和一段随后由其加载的、长度可能较长、存储在其它位置的第二阶段引导程序（second stage bootloader）。</p>
<p>引导程序必须决定内核的位置，并将内核加载到内存。引导程序还需要将<code>CPU</code>从<code>16</code>位的实模式，先切换到<code>32</code>位的保护模式（protected mode），最终切换到<code>64</code>位的长模式（long mode）：此时，所有的<code>64</code>位寄存器和整个主内存（main memory）才能被访问。引导程序的第三个作用，是从<code>BIOS</code>查询特定的信息，并将其传递到内核；如查询和传递内存映射表（memory map）。</p>
<p>编写一个引导程序并不是一个简单的任务，因为这需要使用汇编语言，而且必须经过许多意图并不明显的步骤——比如，把一些魔术数字（magic number）写入某个寄存器。因此，我们不会讲解如何编写自己的引导程序，而是推荐 bootimage 工具——它能够自动并且方便地为你的内核准备一个引导程序。</p>

</details></div><div class="tab-pane" id="lab00-3"><figure class="highlight bash"><figcaption><span>创建相关的Rust项目</span></figcaption><table><tr><td class="code"><pre><span class="line">cargo new os</span><br></pre></td></tr></table></figure>

<p>这里创建的是一个二进制的应用，不是一个库应用</p>
<p>实验指导书要求我们使用<code>RISC-V</code>的指令架构，因此我们需要修改目标平台</p>
<div class="note warning no-icon"><p>注意， <code>Rust</code>查看<code>Target Triplet</code>的方式</p>
<figure class="highlight bash"><figcaption><span>查看代码的一些基础信息</span></figcaption><table><tr><td class="code"><pre><span class="line">rustc --version --verbose</span><br></pre></td></tr></table></figure>
</div>

<figure class="highlight bash"><figcaption><span>修改目标平台</span></figcaption><table><tr><td class="code"><pre><span class="line">cargo run --target riscv64gc-unknown-none-eft</span><br></pre></td></tr></table></figure>

<p>进行上述操作之后， 运行会提示<code>can&#39;t find crate for std</code>，报错的原因是因为目标平台上确实没有<code>std</code>，也不存在任何受<code>OS</code>支持的系统调用。这样的平台就是<strong>裸机平台</strong>。</p>
<p>幸运的是，除了<code>std</code>之外，<code>Rust</code>还有一个不需要任何操作系统支持的核心库<code>core</code>，它包含了<code>Rust</code>语言相当一部分核心机制，可以满足本门课程的需求。有很多第三方库也不依赖标准库，而仅仅依赖核心库。</p>
<p>为了完成上述的配置，尝试移除相关的标准库依赖</p>
<figure class="highlight toml"><figcaption><span>os/.cargo/config</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="section">[build]</span></span><br><span class="line"><span class="attr">target</span> = <span class="string">&quot;riscv64gc-unknown-none-elf&quot;</span></span><br></pre></td></tr></table></figure>

<p>经过如上操作， 将使<code>cargo</code>工具在<code>os</code>目录下默认会使用<code>riscv64gc-unknown-none-elf</code>作为目标平台。 这种编译器运行的平台（<code>x86_64</code>）与可执行文件运行的目标平台不同的情况，称为**交叉编译 (Cross Compile)**。</p>
<p><strong>移除<code>println!</code>宏</strong></p>
<p>需要在<code>main.rs</code>的开头添加上一行<code>#![no_std]</code>，告诉<code>Rust</code>编译器不适用<code>Rust</code>标准库，转而使用核心库。</p>
<p>由于<code>println!</code>宏是由<code>std</code>提供的，并且会使用到一个名为<code>write</code>的系统调用。因此我们在移除标准库之后需要自行定义一个<code>println!</code></p>
<p>在所有的工作开始之前，我们需要为<code>Rust</code>提供错误处理函数<code>#[panic_handler]</code>, 本来 这个功能是由<code>std</code>提供的，但是我们已经把他干掉了，而<code>core</code>并没有提供这个对应的功能，需要自行造的第一辆车了。</p>
<figure class="highlight rust"><figcaption><span>os/src/long_items.rs</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> core::panic:PanicInfo;</span><br><span class="line"></span><br><span class="line"><span class="comment">// `#[panic_handle]`这个标记，是告诉编译器采用自定义的错误处理函数</span></span><br><span class="line"><span class="meta">#[panic_handler]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">panic</span>(_info: &amp;PanicInfo) <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="keyword">loop</span> &#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<p>似乎一切都已经可以了， 但当我们<code>cargo build</code>的时候， 会出现如下</p>
<div class="note danger no-icon"><p>error: requires <code>start</code> lang_item</p>
</div>

<p>阅读编译器我们可以知道，我们缺少了一个名为<code>start</code>的语义项。</p>
<div class="note info no-icon"><p><code>start</code>语义项代表了标准库<code>std</code>在执行应用程序之前需要进行的一些初始化工作。由于我们禁用了标准库，编译器也就找不到这项功能的实现了。</p>
</div>

<figure class="highlight rust"><figcaption><span>os/src/main.rs</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="comment">// ...</span></span><br><span class="line"><span class="meta">#![no_main]</span></span><br><span class="line"><span class="comment">// ...</span></span><br></pre></td></tr></table></figure>

<p>通过在<code>main.rs</code>的开头加入<code>#![no_main]</code>我们可以告知编译器我们的代码没有一般意义上的<code>main</code>函数， 并将原来的<code>main</code>函数删除。这样编译器也就不需要考虑初始化工作了。</p>
<p><strong>简单介绍一下分析<code>rust</code>程序的工具</strong></p>
<details class="note info no-icon"><summary><p><code>rust</code>分析工具</p>
</summary>
<figure class="highlight bash"><figcaption><span>Demo</span></figcaption><table><tr><td class="code"><pre><span class="line">[文件格式]</span><br><span class="line">&gt; file target/riscv64gc-unknown-none-elf/debug/os</span><br><span class="line">target/riscv64gc-unknown-none-elf/debug/os: ELF 64-bit LSB executable, UCB RISC-V, ......</span><br><span class="line"></span><br><span class="line">[文件头信息]</span><br><span class="line">&gt; rust-readobj -h target/riscv64gc-unknown-none-elf/debug/os</span><br><span class="line">   File: target/riscv64gc-unknown-none-elf/debug/os</span><br><span class="line">   Format: elf64-littleriscv</span><br><span class="line">   Arch: riscv64</span><br><span class="line">   AddressSize: 64bit</span><br><span class="line">   ......</span><br><span class="line">   Type: Executable (0x2)</span><br><span class="line">   Machine: EM_RISCV (0xF3)</span><br><span class="line">   Version: 1</span><br><span class="line">   Entry: 0x0</span><br><span class="line">   ......</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">[反汇编导出汇编程序]</span><br><span class="line">&gt; rust-objdump -S target/riscv64gc-unknown-none-elf/debug/os</span><br><span class="line">   target/riscv64gc-unknown-none-elf/debug/os:       file format elf64-littleriscv</span><br></pre></td></tr></table></figure>

</details>

<p><strong>用户态最小执行环境</strong></p>
<p>我们需要为自己的<code>Rust</code>程序提供一个入口函数<code>_start()</code>, 如下代码：</p>
<figure class="highlight rust"><figcaption><span>os/src/main.rs</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="meta">#[no_mangle]</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">_start</span>() &#123;</span><br><span class="line">    <span class="keyword">loop</span> &#123;&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过，反汇编分析工具，我们可以得到（以下参数来自实验指导书）,可以看出两条指令就是一个死循环，这样程序就已经是合理的了。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0000000000011120 &lt;_start&gt;:</span><br><span class="line">;     loop &#123;&#125;</span><br><span class="line">    11120: 09 a0            j       2 &lt;_start+0x2&gt;</span><br><span class="line">    11122: 01 a0            j       0 &lt;_start+0x2&gt;</span><br></pre></td></tr></table></figure>

<p>既然有了开始，那么总是需要能够有个结束的， 当我们把<code>_start()</code>中的<code>loop&#123;&#125;</code>语句注释之后，通过反汇编， 可以得到</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0000000000011120 &lt;_start&gt;:</span><br><span class="line"> ; &#125;</span><br><span class="line">   11120: 82 80              ret</span><br></pre></td></tr></table></figure>

<p>虽然这样看起来是一个合法的程序，但是当通过<code>qemu</code>执行程序的时候，则会出现段错误，具体如下</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&gt; qemu-riscv64 target/riscv64gc-unknown-none-elf/debug/os</span><br><span class="line">   段错误 (核心已转储)</span><br></pre></td></tr></table></figure>

<details class="note info no-icon"><summary><p>QEMU的两种运行模式</p>
</summary>
<ul>
<li><p><code>User mode</code>模式，即用户态模拟，如<code>qemu-riscv64</code>程序， 能够模拟不同处理器的用户态指令的执行，并可以直接解析ELF可执行文件， 加载运行那些为不同处理器编译的用户级Linux应用程序。</p>
</li>
<li><p><code>System mode</code>模式，即系统态模式，如<code>qemu-system-riscv64</code>程序， 能够模拟一个完整的基于不同CPU的硬件系统，包括处理器、内存及其他外部设备，支持运行完整的操作系统。</p>
</li>
</ul>

</details>

<p>目前的执行环境还缺少一个推出的机制，我们需要系统提供<code>exit</code>系统调用来推出程序，下面是主要的代码:</p>
<figure class="highlight rust"><figcaption><span>os/src/main.rs</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> SYSCALL_EXIT: <span class="type">usize</span> = <span class="number">93</span>;</span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">syscall</span>(id: <span class="type">usize</span>, args: [<span class="type">usize</span>; <span class="number">3</span>]) <span class="punctuation">-&gt;</span> <span class="type">isize</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">ret</span>;</span><br><span class="line">    <span class="keyword">unsafe</span> &#123;</span><br><span class="line">        core::arch::asm!(</span><br><span class="line">            <span class="string">&quot;ecall&quot;</span>,</span><br><span class="line">            <span class="title function_ invoke__">inlateout</span>(<span class="string">&quot;x10&quot;</span>) args[<span class="number">0</span>] =&gt; ret,</span><br><span class="line">            <span class="title function_ invoke__">in</span>(<span class="string">&quot;x11&quot;</span>) args[<span class="number">1</span>],</span><br><span class="line">            <span class="title function_ invoke__">in</span>(<span class="string">&quot;x12&quot;</span>) args[<span class="number">2</span>],</span><br><span class="line">            <span class="title function_ invoke__">in</span>(<span class="string">&quot;x17&quot;</span>) id,</span><br><span class="line">        );</span><br><span class="line">    &#125;</span><br><span class="line">    ret</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">sys_exit</span>(xstate: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">isize</span> &#123;</span><br><span class="line">    <span class="title function_ invoke__">syscall</span>(SYSCALL_EXIT, [xstate <span class="keyword">as</span> <span class="type">usize</span>, <span class="number">0</span>, <span class="number">0</span>])</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[no_magle]</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">_start</span>() &#123;</span><br><span class="line">    <span class="title function_ invoke__">sys_exit</span>(<span class="number">9</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>main.rs</code>增加的内容不多，但还是有点与一般的应用程序有所不同，因为它引入了汇编和系统调用。 第二章的第二节 实现应用程序 会详细介绍上述代码的含义。 这里读者只需要知道<code>_start</code>函数调用了一个<code>sys_exit</code>函数， 向操作系统发出了退出的系统调用请求，退出码为<code>9</code>。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&gt; cargo build --target riscv64gc-unknown-none-elf</span><br><span class="line">&gt; qemu-riscv64 target/riscv64gc-unknown-none-elf/debug/os; <span class="built_in">echo</span> $?</span><br><span class="line">    9</span><br></pre></td></tr></table></figure>

<p>从中可以看到，返回的结果确实是<code>9</code>。这样基本就已经完成了一个简陋的用户态最小化执行环境。</p>
<p><strong>有显示支持的用户态执行环境</strong></p>
<p>没有<code>println!</code>输出信息，终究还是少了一些， <code>Rust</code>的<code>core</code>库内建了一系列帮助实现显示字符的基本<code>Trait</code>和数据结构，函数等，我们可以对其中的关键部分进行扩展，就可以实现定制的<code>println!</code>功能。</p>
<p><em>实现输出字符串的相关函数</em></p>
<p>首先封装以下对<code>SYSCALL_WRITE</code>系统调用</p>
<figure class="highlight rust"><figcaption><span>os/src/main.rs</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> SYSCALL_WRITE: <span class="type">usize</span> =  <span class="number">64</span>;</span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">sys_wirte</span>(fd: <span class="type">usize</span>, buffer: &amp;[<span class="type">u8</span>]) <span class="punctuation">-&gt;</span> <span class="type">isize</span> &#123;</span><br><span class="line">    <span class="title function_ invoke__">syscall</span>(SYSCALL_WRITE, [fd, buffer.<span class="title function_ invoke__">as_ptr</span>() <span class="keyword">as</span> <span class="type">usize</span>, buffer.<span class="title function_ invoke__">len</span>()])</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>软后实现基于<code>Write</code>的<code>Trait</code>的数据结构，并完成<code>Write Trait</code>所需要的<code>write_str</code>函数，并用<code>print</code>函数进行封装。</p>
<figure class="highlight rust"><figcaption><span>os/src/console.rs</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Stdout</span>;</span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">Wirte</span> <span class="keyword">for</span> <span class="title class_">Stdout</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">wirte_str</span>(&amp;<span class="keyword">mut</span> <span class="keyword">self</span>, s: &amp;<span class="type">str</span>) <span class="punctuation">-&gt;</span> fmt::<span class="type">Result</span> &#123;</span><br><span class="line">        <span class="title function_ invoke__">sys_write</span>(<span class="number">1</span>, s.<span class="title function_ invoke__">as_bytes</span>());</span><br><span class="line">        <span class="title function_ invoke__">Ok</span>(())</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">print</span>(args: fmt::Arguments) &#123;</span><br><span class="line">    Stdout.<span class="title function_ invoke__">write_fmt</span>(args).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后，实现基于<code>print</code>函数，实现<code>Rust</code>语言的格式化宏</p>
<figure class="highlight rust"><figcaption><span>os/src/console.rs</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="built_in">macro_rules!</span> print &#123;</span><br><span class="line">    ($fmt: literal $(, $($arg: tt)+)?) =&gt; &#123;</span><br><span class="line">        $crate::console::<span class="title function_ invoke__">print</span>(<span class="built_in">format_args!</span>($fmt $(, $($arg)+)?));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">macro_rules!</span> println &#123;</span><br><span class="line">    ($fmt: literal $(, $($arg: tt)+)?) =&gt; &#123;</span><br><span class="line">      $crate::console::<span class="title function_ invoke__">print</span>(<span class="built_in">format_args!</span>(<span class="built_in">concat!</span>($fmt, <span class="string">&quot;\n&quot;</span>) $(, $($arg)+)?));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>再次重新编译，我们就可以看到相应的输出</p>
<p>至此，我们已经实现了用户态最小的执行环境。</p>
<p><strong>构建裸机执行环境</strong></p>
<p>用<code>QEMU</code>软件<code>qemu-system-riscv64</code>来模拟<code>RISC-V 64</code>计算机。加载内核程序的命令如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">qemu-system-riscv64 \</span><br><span class="line">            -machine virt \</span><br><span class="line">            -nographic \</span><br><span class="line">            -bios $(BOOTLOADER) \</span><br><span class="line">            -device loader,file=$(KERNEL_BIN),addr=$(KERNEL_ENTRY_PA)</span><br></pre></td></tr></table></figure>

<ul>
<li><p><code>-bios $(BOOTLOADER)</code> 意味着硬件加载了一个<code>BootLoader</code>程序，即<code>RustSBI</code></p>
</li>
<li><p><code>-device loader,file=$(KERNEL_BIN),addr=$(KERNEL_ENTRY_PA)</code>表示硬件内存中的特定位置<code>$(KERNEL_ENTRY_PA)</code>放置了操作系统的二进制代码<code>$(KERNEL_BIN)</code>。<code>$(KERNEL_ENTRY_PA)</code>的值是<code>0x80200000</code>。</p>
</li>
</ul>
<p>当我们执行包含上述启动参数的<code>qemu-system-riscv64</code>软件，就意味给这台虚拟的<code>RISC-V 64</code>计算机加电了。 此时，CPU 的其它通用寄存器清零，而 PC 会指向<code>0x1000</code>的位置，这里有固化在硬件中的一小段引导代码， 它会很快跳转到<code>0x80000000</code>的<code>RustSBI</code>处。<code>RustSBI</code>完成硬件初始化后，会跳转到<code>$(KERNEL_BIN)</code>所在内存位置<code>0x80200000</code>处， 执行操作系统的第一条指令。&#96;</p>
<details class="note info no-icon"><summary><p>RustSBI是什么</p>
</summary>
<p><code>SBI</code>是<code>RISC-V</code>的一种底层规范，<code>RustSBI</code>是它的一种实现。 操作系统内核与<code>RustSBI</code>的关系有点像应用与操作系统内核的关系，后者向前者提供一定的服务。只是<code>SBI</code>提供的服务很少， 比如关机，显示字符串等。</p>

</details>

<p><strong>实现关机功能</strong></p>
<p>对上一节实现的代码稍作调整，通过<code>ecall</code>调用<code>RustSBI</code>实现关机功能：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// bootloader/rustsbi-qemu.bin 直接添加的SBI规范实现的二进制代码，给操作系统提供基本支持服务</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// os/src/sbi.rs</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">sbi_call</span>(which: <span class="type">usize</span>, arg0: <span class="type">usize</span>, arg1: <span class="type">usize</span>, arg2: <span class="type">usize</span>) <span class="punctuation">-&gt;</span> <span class="type">usize</span> &#123;</span><br><span class="line"> <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">ret</span>;</span><br><span class="line">  <span class="keyword">unsafe</span> &#123;</span><br><span class="line">      core::arch::asm!(</span><br><span class="line">          <span class="string">&quot;ecall&quot;</span>,</span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> SBI_SHUTDOWN: <span class="type">usize</span> = <span class="number">8</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">shutdown</span>() <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="title function_ invoke__">sbi_call</span>(SBI_SHUTDOWN, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">panic!</span>(<span class="string">&quot;It should shutdown!&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// os/src/main.rs</span></span><br><span class="line"><span class="meta">#[no_mangle]</span></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">_start</span>() &#123;</span><br><span class="line">    <span class="title function_ invoke__">shutdown</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>应用程序访问操作系统提供的系统调用的指令是<code>ecall</code>，操作系统访问<code>RustSBI</code>提供的<code>SBI</code>调用的指令也是<code>ecall</code>， 虽然指令一样，但它们所在的特权级是不一样的。 简单地说，应用程序位于最弱的用户特权级（<code>User Mode</code>）， 操作系统位于内核特权级（<code>Supervisor Mode</code>），<code>RustSBI</code>位于机器特权级（<code>Machine Mode</code>）。 </p>
<p><em>编译执行，结果如下</em></p>
<div class="note info no-icon"><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\ 编译生成ELF格式的执行文件</span></span><br><span class="line">&gt; cargo build --release</span><br><span class="line"> Compiling os v0.1.0 (/media/chyyuu/ca8c7ba6-51b7-41fc-8430-e29e31e5328f/thecode/rust/os_kernel_lab/os)</span><br><span class="line">  Finished release [optimized] target(s) <span class="keyword">in</span> 0.15s</span><br><span class="line"><span class="comment">#\ 把ELF执行文件转成bianary文件</span></span><br><span class="line">&gt; rust-objcopy --binary-architecture=riscv64 target/riscv64gc-unknown-none-elf/release/os --strip-all -O binary target/riscv64gc-unknown-none-elf/release/os.bin</span><br><span class="line"></span><br><span class="line"><span class="comment">#\ 加载运行</span></span><br><span class="line">&gt; qemu-system-riscv64 -machine virt -nographic -bios ../bootloader/rustsbi-qemu.bin -device loader,file=target/riscv64gc-unknown-none-elf/release/os.bin,addr=0x80200000</span><br><span class="line"><span class="comment">#\ 无法退出，风扇狂转，感觉碰到死循环</span></span><br></pre></td></tr></table></figure>
</div>

<p>问题在哪？通过<code>rust-readobj</code>分析<code>os</code>可执行程序，发现其入口地址不是<code>RustSBI</code>约定的<code>0x80200000</code>。我们需要修改程序的内存布局并设置好栈空间。</p>
<p><strong>设置正确的程序内存布局</strong></p>
<div class="note info no-icon"><p>可以通过 链接脚本 (<strong>Linker Script</strong>) 调整链接器的行为，使得最终生成的可执行文件的内存布局符合我们的预期。</p>
</div>

<p>修改<code>Cargo</code>的配置文件来使用我们自己的链接脚本<code>os/src/linker.ld</code>：</p>
<figure class="highlight toml"><figcaption><span>os/.cargo/config</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="section">[build]</span></span><br><span class="line"><span class="attr">target</span> = <span class="string">&quot;riscv64gc-unknown-none-elf&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[target.riscv64gc-unknown-none-elf]</span></span><br><span class="line"><span class="attr">rustflags</span> = [</span><br><span class="line">    <span class="string">&quot;-Clink-arg=-Tsrc/linker.ld&quot;</span>, <span class="string">&quot;-Cforce-frame-pointers=yes&quot;</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>具体的链接脚本<code>os/src/linker.ld</code>如下：</p>
<figure class="highlight bash"><figcaption><span>os/src/linker.ld</span></figcaption><table><tr><td class="code"><pre><span class="line">OUTPUT_ARCH(riscv)</span><br><span class="line">ENTRY(_start)</span><br><span class="line">BASE_ADDRESS = 0x80200000;</span><br><span class="line"></span><br><span class="line">SECTIONS</span><br><span class="line">&#123;</span><br><span class="line">    . = BASE_ADDRESS;</span><br><span class="line">    skernel = .;</span><br><span class="line"></span><br><span class="line">    stext = .;</span><br><span class="line">    .text : &#123;</span><br><span class="line">        *(.text.entry)</span><br><span class="line">        *(.text .text.*)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    . = ALIGN(4K);</span><br><span class="line">    etext = .;</span><br><span class="line">    srodata = .;</span><br><span class="line">    .rodata : &#123;</span><br><span class="line">        *(.rodata .rodata.*)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    . = ALIGN(4K);</span><br><span class="line">    erodata = .;</span><br><span class="line">    sdata = .;</span><br><span class="line">    .data : &#123;</span><br><span class="line">        *(.data .data.*)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    . = ALIGN(4K);</span><br><span class="line">    edata = .;</span><br><span class="line">    .bss : &#123;</span><br><span class="line">        *(.bss.stack)</span><br><span class="line">        sbss = .;</span><br><span class="line">        *(.bss .bss.*)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    . = ALIGN(4K);</span><br><span class="line">    ebss = .;</span><br><span class="line">    ekernel = .;</span><br><span class="line"></span><br><span class="line">    /DISCARD/ : &#123;</span><br><span class="line">        *(.eh_frame)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第<code>1</code>行我们设置了目标平台为<code>riscv</code>；第<code>2</code>行我们设置了整个程序的入口点为之前定义的全局符号<code>_start</code>； 第<code>3</code>行定义了一个常量<code>BASE_ADDRESS</code>为<code>0x80200000</code>，<code>RustSBI</code>期望的<code>OS</code>起始地址；</p>
<p>从<code>BASE_ADDRESS</code>开始，代码段<code>.text</code>, 只读数据段<code>.rodata</code>，数据段<code>.data</code>,<code>bss</code>段<code>.bss</code>由低到高依次放置， 且每个段都有两个全局变量给出其起始和结束地址（比如<code>.text</code>段的开始和结束地址分别是<code>stext</code>和<code>etext</code>）。</p>
<p><strong>正确配置栈空间</strong></p>
<figure class="highlight bash"><figcaption><span>os/src/entry.asm</span></figcaption><table><tr><td class="code"><pre><span class="line">    .section .text.entry</span><br><span class="line">    .globl _start</span><br><span class="line">_start:</span><br><span class="line">    la sp, boot_stack_top</span><br><span class="line">    call rust_main</span><br><span class="line"></span><br><span class="line">    .section .bss.stack</span><br><span class="line">    .globl boot_stack</span><br><span class="line">boot_stack:</span><br><span class="line">    .space 4096 * 16</span><br><span class="line">    .globl boot_stack_top</span><br><span class="line">boot_stack_top:</span><br></pre></td></tr></table></figure>

<p>在第 8 行，我们预留了一块大小为 <code>4096 * 16</code> 字节，也就是 64KiB 的空间， 用作操作系统的栈空间。 栈顶地址被全局符号 <code>boot_stack_top</code> 标识，栈底则被全局符号 <code>boot_stack</code> 标识。 同时，这块栈空间被命名为 <code>.bss.stack</code> ，链接脚本里有它的位置。</p>
<p><code>_start</code> 作为操作系统的入口地址，将依据链接脚本被放在 <code>BASE_ADDRESS</code> 处。 <code>la sp</code>, <code>boot_stack_top</code> 作为 OS 的第一条指令， 将 <code>sp</code> 设置为栈空间的栈顶。 简单起见，我们目前不考虑 <code>sp</code> 越过栈底 <code>boot_stack</code> ，也就是栈溢出的情形。 第二条指令则是函数调用 <code>rust_main</code> ，这里的 <code>rust_main</code> 是我们稍后自己编写的应用入口。</p>
<p>接着，我们在 <code>main.rs</code> 中嵌入这些汇编代码并声明应用入口 <code>rust_main </code>：</p>
<figure class="highlight rust"><figcaption><span>os/src/main.rs</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="meta">#![no_std]</span></span><br><span class="line"><span class="meta">#![no_main]</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">mod</span> lang_items;</span><br><span class="line"></span><br><span class="line">core::arch::global_asm!(include_srt!(<span class="string">&quot;entry.asm&quot;</span>));</span><br><span class="line"></span><br><span class="line"><span class="meta">#[no_mangle]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">rust_main</span>() <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="title function_ invoke__">shutdown</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第 7 行，我们使用 <code>global_asm</code> 宏，将同目录下的汇编文件 <code>entry.asm</code> 嵌入到代码中。</p>
<p>从第 9 行开始， 我们声明了应用的入口点 <code>rust_main</code> ，需要注意的是，这里通过宏将 <code>rust_main</code> 标记为 <code>#[no_mangle]</code> 以避免编译器对它的名字进行混淆，不然在链接时，<code> entry.asm</code> 将找不到 <code>main.rs</code> 提供的外部符号 <code>rust_main</code>，导致链接失败。</p>
<p>再次使用上节中的编译，生成和运行操作，我们看到QEMU模拟的RISC-V 64计算机 优雅 地退出了！</p>
<p><strong>清空 <code>.bss</code>段</strong></p>
<p>等一等，与内存相关的部分太容易出错了， <strong>清零 .bss 段</strong> 的工作我们还没有完成。</p>
<figure class="highlight rust"><figcaption><span>os/src/main.rs</span></figcaption><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">clear_bass</span>() &#123;</span><br><span class="line">    <span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> &#123;</span><br><span class="line">        <span class="keyword">fn</span> <span class="title function_">sbss</span>();</span><br><span class="line">        <span class="keyword">fn</span> <span class="title function_">ebss</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    (sbss <span class="keyword">as</span> <span class="type">usize</span>..ebss <span class="keyword">as</span> <span class="type">usize</span>).<span class="title function_ invoke__">for_each</span>(|a| &#123;</span><br><span class="line">        <span class="keyword">unsafe</span> &#123; (a <span class="keyword">as</span> *<span class="keyword">mut</span> <span class="type">u8</span>).<span class="title function_ invoke__">write_volatile</span>(<span class="number">0</span>) &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">rust_main</span>() <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="title function_ invoke__">clear_bass</span>();</span><br><span class="line">    <span class="title function_ invoke__">shutdown</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<p>链接脚本 <code>linker.ld</code> 中给出的全局符号 <code>sbss</code> 和 <code>ebss</code> 让我们能轻松确定 <code>.bss</code> 段的位置。</p>
<p><strong>添加裸机打印相关函数</strong></p>
<p>在上一节中我们为用户态程序实现的 <code>println</code> 宏，略作修改即可用于本节的内核态操作系统。 详见 <code>os/src/console.rs</code>。</p>
<p>利用 <code>println</code> 宏，我们重写异常处理函数 <code>panic</code>，使其在 panic 时能打印错误发生的位置。 相关代码位于 <code>os/src/lang_items.rs</code> 中。</p>
<p>我们还使用第三方库 <code>log</code> 为你实现了日志模块，相关代码位于 <code>os/src/logging.rs</code> 中。</p></div><div class="tab-pane" id="lab00-4"><div class="note info no-icon"><p>个人觉得，CI的这种策略真的是不错，很有自动化的味道，所有检测所需的动作，可以以<code>config</code>的方式完整的确认下来，真的是一个不错的体验。</p>
</div></div></div></div></div><div class="tab-pane" id="lab-2"><div class="tabs" id="01"><ul class="nav-tabs"><li class="tab active"><a href="#01-1">实验描述</a></li><li class="tab"><a href="#01-2">实验问题</a></li><li class="tab"><a href="#01-3">实验报告</a></li><li class="tab"><a href="#01-4">实验体会</a></li></ul><div class="tab-content"><div class="tab-pane active" id="01-1"></div><div class="tab-pane" id="01-2"></div><div class="tab-pane" id="01-3"></div><div class="tab-pane" id="01-4"></div></div></div></div><div class="tab-pane" id="lab-3"><div class="tabs" id="lab_1"><ul class="nav-tabs"><li class="tab active"><a href="#lab_1-1">实验描述</a></li><li class="tab"><a href="#lab_1-2">实验问题</a></li><li class="tab"><a href="#lab_1-3">实验报告</a></li><li class="tab"><a href="#lab_1-4">实验体会</a></li></ul><div class="tab-content"><div class="tab-pane active" id="lab_1-1"></div><div class="tab-pane" id="lab_1-2"></div><div class="tab-pane" id="lab_1-3"></div><div class="tab-pane" id="lab_1-4"></div></div></div></div><div class="tab-pane" id="lab-4"><div class="tabs" id="lab_2"><ul class="nav-tabs"><li class="tab active"><a href="#lab_2-1">实验描述</a></li><li class="tab"><a href="#lab_2-2">实验问题</a></li><li class="tab"><a href="#lab_2-3">实验报告</a></li><li class="tab"><a href="#lab_2-4">实验体会</a></li></ul><div class="tab-content"><div class="tab-pane active" id="lab_2-1"></div><div class="tab-pane" id="lab_2-2"></div><div class="tab-pane" id="lab_2-3"></div><div class="tab-pane" id="lab_2-4"></div></div></div></div><div class="tab-pane" id="lab-5"><div class="tabs" id="lab_3"><ul class="nav-tabs"><li class="tab active"><a href="#lab_3-1">实验描述</a></li><li class="tab"><a href="#lab_3-2">实验问题</a></li><li class="tab"><a href="#lab_3-3">实验报告</a></li><li class="tab"><a href="#lab_3-4">实验体会</a></li></ul><div class="tab-content"><div class="tab-pane active" id="lab_3-1"></div><div class="tab-pane" id="lab_3-2"></div><div class="tab-pane" id="lab_3-3"></div><div class="tab-pane" id="lab_3-4"></div></div></div></div><div class="tab-pane" id="lab-6"><div class="tabs" id="lab_4"><ul class="nav-tabs"><li class="tab active"><a href="#lab_4-1">实验描述</a></li><li class="tab"><a href="#lab_4-2">实验问题</a></li><li class="tab"><a href="#lab_4-3">实验报告</a></li><li class="tab"><a href="#lab_4-4">实验体会</a></li></ul><div class="tab-content"><div class="tab-pane active" id="lab_4-1"></div><div class="tab-pane" id="lab_4-2"></div><div class="tab-pane" id="lab_4-3"></div><div class="tab-pane" id="lab_4-4"></div></div></div></div><div class="tab-pane" id="lab-7"><div class="tabs" id="lab_5"><ul class="nav-tabs"><li class="tab active"><a href="#lab_5-1">实验描述</a></li><li class="tab"><a href="#lab_5-2">实验问题</a></li><li class="tab"><a href="#lab_5-3">实验报告</a></li><li class="tab"><a href="#lab_5-4">实验体会</a></li></ul><div class="tab-content"><div class="tab-pane active" id="lab_5-1"></div><div class="tab-pane" id="lab_5-2"></div><div class="tab-pane" id="lab_5-3"></div><div class="tab-pane" id="lab_5-4"></div></div></div></div></div></div>

</details>

<h2 id="第一階段的工作日記"><a href="#第一階段的工作日記" class="headerlink" title="第一階段的工作日記"></a>第一階段的工作日記</h2><p>（始于 2022年7月6日）</p>
<details class="note primary"><summary><p>工作日記</p>
</summary>
<ul>
<li>2022-7-5</li>
</ul>
<p><strong>主要完成了 rustlings 的部分練習</strong></p>
<table>
<thead>
<tr>
<th>内容部分</th>
<th>完成情況</th>
<th>備注</th>
</tr>
</thead>
<tbody><tr>
<td>if</td>
<td>完成</td>
<td>簡單</td>
</tr>
<tr>
<td>intro</td>
<td>完成</td>
<td>簡單</td>
</tr>
<tr>
<td>structs</td>
<td>完成</td>
<td>簡單</td>
</tr>
<tr>
<td>variables</td>
<td>完成</td>
<td>編譯器，教我如何更改代碼</td>
</tr>
</tbody></table>
<p><strong>配置了本地的測試環境 – Docker</strong></p>
<blockquote>
<p>非常難受，非常難受的是： 如果不是開了代理這個本地環境幾乎不能完成配置</p>
</blockquote>
<p><strong>有一點想吐槽的點</strong></p>
<blockquote>
<p>Github 的 codespace 好像不是很穩定，或者説好像并發情況下表現不是很好</p>
</blockquote>
<ul>
<li>2022-7-6</li>
</ul>
<p>主要就是改善一下<code>Dockerfile</code>的相關配置，<code>fork</code>了官方的template, 並提交了<code>pull request</code>。</p>
<ul>
<li>2022-7-7</li>
</ul>
<p>提交 Lab0-0 开始 Lab0-1</p>
<blockquote>
<p>主要困在了 <code>cl</code>的部分，没事后面解决了</p>
</blockquote>
<ul>
<li>2022-7-8</li>
</ul>
<p>开始 Lab0-1, 整理了一下自己的博客布局</p>
<ul>
<li>2022-7-9</li>
</ul>
<p>完成rustlings</p>
<ul>
<li>2022-7-10</li>
</ul>
<p>学习 RISC-V， 整理rustlings, </p>
<ul>
<li>2022-7-11</li>
</ul>
<p>完成Lab，学习RISC-V，整理rustlings, </p>
<ul>
<li>2022-7-12</li>
</ul>
<p>学习RISC-V，整理rustlings, 和Lab</p>
<ul>
<li>2022-7-13</li>
</ul>
<p>学习RISC-V，整理rustlings, 和Lab</p>
<ul>
<li>2022-7-14</li>
</ul>
<p>学习RISC-V，整理rustlings, 和Lab</p>
<ul>
<li>2022-7-15</li>
</ul>
<p>做一些和整理有关的工作</p>
<ul>
<li>2022-7-16</li>
</ul>
<p>做一些和整理有关的工作</p>
<ul>
<li>2022-7-17</li>
</ul>
<p>做一些和整理有关的工作</p>
<ul>
<li>2022-7-18</li>
</ul>
<p>做一些和整理有关的工作</p>
<ul>
<li>2022-7-19</li>
</ul>
<p>做一些和整理有关的工作</p>
<ul>
<li>2022-7-20</li>
</ul>
<p>做一些和整理有关的工作</p>

</details>

<hr>
<h2 id="Rust-的相關筆記"><a href="#Rust-的相關筆記" class="headerlink" title="Rust 的相關筆記"></a>Rust 的相關筆記</h2><hr>
<h2 id="RISC-V-的相關筆記"><a href="#RISC-V-的相關筆記" class="headerlink" title="RISC-V 的相關筆記"></a>RISC-V 的相關筆記</h2><hr>
<h1 id="第二階段的學習記錄"><a href="#第二階段的學習記錄" class="headerlink" title="第二階段的學習記錄"></a>第二階段的學習記錄</h1><details class="note default"><summary><p>第二階段的一些記錄</p>
</summary>
<div class="tabs" id="secondrecordtags"><ul class="nav-tabs"><li class="tab active"><a href="#secondrecordtags-1">心得</a></li></ul><div class="tab-content"><div class="tab-pane active" id="secondrecordtags-1"><ol>
<li>關於任務和實習</li>
</ol></div></div></div>

</details>


<h2 id="第二階段的工作日記"><a href="#第二階段的工作日記" class="headerlink" title="第二階段的工作日記"></a>第二階段的工作日記</h2><details class="note primary"><summary><p>工作日記</p>
</summary>
<p>2022年8月01日 ~ 2022年8月21日 : 工作進度幾乎為零， 主要是實習總是和訓練營的工作產生了比較嚴重的衝突</p>
<ul>
<li>2022年8月22日</li>
</ul>

</details>

<h1 id="一些技術上的問題"><a href="#一些技術上的問題" class="headerlink" title="一些技術上的問題"></a>一些技術上的問題</h1><div class="tabs" id="question"><ul class="nav-tabs"><li class="tab active"><a href="#question-1">实验环境的问题</a></li><li class="tab"><a href="#question-2">测试的一些建议</a></li><li class="tab"><a href="#question-3">Question 3</a></li></ul><div class="tab-content"><div class="tab-pane active" id="question-1"><p><strong>实验环境产生的问题</strong></p>
<div class="note default"><p>因为个人使用Docker部署实验环境， 这一部分是总结实验环境产生的一些问题，并尝试给出一些解决方案</p>
</div>

<div class="tabs" id="sub-sub-category"><ul class="nav-tabs"><li class="tab active"><a href="#sub-sub-category-1">Ubuntu部分的问题</a></li><li class="tab"><a href="#sub-sub-category-2">Cargo部分的问题</a></li><li class="tab"><a href="#sub-sub-category-3">Qemu部分的问题</a></li></ul><div class="tab-content"><div class="tab-pane active" id="sub-sub-category-1"><p>出於對操作複雜度的考慮，個人考慮使用<code>Docker</code>進行實驗環境的部署，從官方的文檔來看，只需要兩個命令<code>make build_docker</code>和<code>make docker</code>就可以完成實驗的部署。</p>
<p>因爲官方提供的Docker的基礎鏡像是<code>Ubuntu:20.04</code>，如果使用默認的官方鏡像，則在網絡不好的情況下，<code>apt-get</code>這個速度會十分緩慢，因此需要對基礎的<code>Ubuntu:20.04</code>鏡像做一些修改：</p>
<p>首先，測試了一下，基礎的<code>Ubuntu:20.04</code>中存在<code>sed</code>命令，可以通過這個命令，修改<code>/etc/apt/sources.list</code>中的鏡像源的列表，爲了簡化操作的複雜度，我添加了一些文件為基礎的文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\ mkdir docker</span></span><br><span class="line"><span class="comment">#\ thouch registryList</span></span><br><span class="line">(root of pj)/docker/registryList</span><br></pre></td></tr></table></figure>

<p>在<code>registryList</code>中輸入相應的<code>Ubuntu 20.04</code>的相關鏡像源。</p>
<p>爲了能在<code>make build_docker</code>中使得自己的鏡像可以生效，需要對根目錄下的<code>Dockerfile</code>進行些許修改:</p>
<figure class="highlight plaintext"><figcaption><span>Dockerfile</span><a href="https://github.com/Andre-HJR/rust-based-os-comp2022/blob/3388dcc008b6efe897c17381d1b917245b18d719/Dockerfile#L12">Dockerfile</a></figcaption><table><tr><td class="code"><pre><span class="line">#\ -1. (Optional) Add Ubuntu Repo</span><br><span class="line">ADD docker/registryList /etc/apt/</span><br><span class="line">RUN sed -i &quot;1r /etc/apt/registryList&quot; /etc/apt/sources.list</span><br></pre></td></tr></table></figure></div><div class="tab-pane" id="sub-sub-category-2"><p>出於對操作複雜度的考慮，個人考慮使用<code>Docker</code>進行實驗環境的部署，從官方的文檔來看，只需要兩個命令<code>make build_docker</code>和<code>make docker</code>就可以完成實驗的部署。</p>
<p>因爲官方提供的Dockerfile，其中<code>Cargo</code>并没有修改为国内的源，如果使用默認的官方鏡像，則在網絡不好的情況下，<code>cargo install</code>這個速度會十分緩慢，因此需要對基礎的<code>cargo</code>鏡像做一些修改：</p>
<p>首先，測試了一下，基礎的<code>Ubuntu:20.04</code>中存在<code>sed</code>命令，可以通過這個命令，修改<code>/root/.cargo/config</code>中的鏡像源的列表，爲了簡化操作的複雜度，我同样添加了一些文件為基礎的文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\ mkdir cargo</span></span><br><span class="line"><span class="comment">#\ thouch config</span></span><br><span class="line">(root of pj)/cargo/config</span><br></pre></td></tr></table></figure>

<p>在<code>config</code>中輸入相應的<code>cargo</code>的相關鏡像源。</p>
<p>爲了能在<code>make build_docker</code>中使得自己的鏡像可以生效，需要對根目錄下的<code>Dockerfile</code>進行些許修改:</p>
<figure class="highlight plaintext"><figcaption><span>Dockerfile</span></figcaption><table><tr><td class="code"><pre><span class="line">#\ -1.1. (Optional) Add cargo Repo</span><br><span class="line">ADD cargo/config /root/.cargo/</span><br></pre></td></tr></table></figure></div><div class="tab-pane" id="sub-sub-category-3"><div class="note info no-icon"><p>其实，这主要是系统构建（<code>make build_docker</code>）的时候产生的问题，准确来说，这可能并不是问题，只是一个优化罢了。</p>
</div>

<p>主要是由于每次在系统构建的时候，镜像总是要从qemu的官网上下载，可能是由于国外的原因， 这个<code>*.tar.xz</code>的压缩包，下载的特别慢，后来我用迅雷下了一下，只需要1分钟不到就全部下载到本地了，压缩包也就2MB大小左右，为了能快一点构建镜像，我觉得可以把压缩包直接放在仓库中，直接解压在镜像里，这样可不更快吗（后面我试着重新构建了一下，确实是这样的）？</p>
<p>于是便有了如下操作：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\ mkdir qemu</span></span><br><span class="line"><span class="comment">#\ move the *.tar.xz to qemu</span></span><br></pre></td></tr></table></figure>

<p>通过<code>ADD</code>将压缩包，加入<code>qemu</code>中，</p>
<figure class="highlight plaintext"><figcaption><span>Dockerfile</span></figcaption><table><tr><td class="code"><pre><span class="line">ADD qemu/qemu-7.0.0.tar.xz /root/</span><br></pre></td></tr></table></figure>

<details class="note warning no-icon"><summary><p><code>ADD</code>和<code>COPY</code>的区别</p>
</summary>
<p><strong><code>ADD</code>和<code>COPY</code>的区别</strong></p>
<ul>
<li><p><code>ADD</code>， <code>ADD</code>命令相对于<code>COPY</code>命令，可以解压缩文件并把它们添加到镜像中的功能，如果我们有一个压缩文件包，并且需要把这个压缩包中的文件添加到镜像中。同时<code>ADD</code>还可以从<code>url</code>拷贝文件到镜像中，但官方不推荐这样使用，官方建议我们当需要从远程复制文件时，最好使用<code>curl</code>或<code>wget</code>命令来代替<code>ADD</code>命令。原因是，当使用<code>ADD</code>命令时，会创建更多的镜像层，当然镜像的<code>size</code>也会更大。</p>
</li>
<li><p><code>COPY</code>，<code>COPY</code>命令用于将于<code>Dockerfile</code>所在目录中的文件在镜像构建阶段从宿主机拷贝到镜像中，对于文件而言可以直接将文件复制到镜像中，对于目录而言，该命令只复制目录中的内容而不包含目录自身。</p>
</li>
</ul>

</details></div></div></div></div><div class="tab-pane" id="question-2"><details class="note info no-icon"><summary><p>让测试更加像一个测试</p>
</summary>
<p>Rust有一个内置的测试框架（built-in test framework）：无需任何设置就可以进行单元测试，只需要创建一个通过<code>assert</code>来检查结果的函数并在函数的头部加上<code>#[test]</code>属性即可。然后<code>cargo test</code>会自动找到并执行你的<code>crate</code>中的所有测试函数。</p>
<p>不幸的是，对于一个<code>no_std</code>的应用，比如我们的内核，这有点点复杂。现在的问题是，Rust的测试框架会隐式的调用内置的<code>test</code>库，但是这个<strong>库依赖于标准库</strong>。这也就是说我们的<code>#[no_std]</code>内核无法使用默认的测试框架。</p>
<p>因此比较好的，或者推荐的是，我们自己实现一个<code>crate</code>让测试更加具有<code>Rust</code>的味道。</p>

</details>

<div class="tabs" id="rtest"><ul class="nav-tabs"><li class="tab active"><a href="#rtest-1">Rust中的测试</a></li><li class="tab"><a href="#rtest-2">退出QEMU</a></li><li class="tab"><a href="#rtest-3">打印到控制台</a></li><li class="tab"><a href="#rtest-4">集成测试</a></li></ul><div class="tab-content"><div class="tab-pane active" id="rtest-1"><p>Rust有一个内置的测试框架（built-in test framework）：无需任何设置就可以进行单元测试，只需要创建一个通过<code>assert</code>来检查结果的函数并在函数的头部加上<code>#[test]</code>属性即可。然后<code>cargo test</code>会自动找到并执行你的<code>crate</code>中的所有测试函数。</p>
<p>不幸的是，对于一个<code>no_std</code>的应用，比如我们的内核，这有点点复杂。现在的问题是，Rust的测试框架会隐式的调用内置的<code>test</code>库，但是这个<strong>库依赖于标准库</strong>。这也就是说我们的<code>#[no_std]</code>内核无法使用默认的测试框架。</p>
<p>当我们试图在我们的项目中执行<code>cargo xtest</code>时，我们可以看到如下信息:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cargo xtest</span><br><span class="line">...</span><br><span class="line">	error[E0463]: can<span class="string">&#x27;t find crate for `test`</span></span><br></pre></td></tr></table></figure>

<p>由于<code>test crate</code>依赖于标准库，所以它在我们的裸机目标上并不可用。虽然将<code>test crate</code>移植到一个<code>#[no_std]</code>上下文环境中是可能的，但是这样做是高度不稳定的并且还会需要一些特殊的<code>hacks</code>，例如重定义<code>panic</code>宏。</p>
<p><strong>自定义测试框架</strong></p>
<p>幸运的是，Rust支持通过使用不稳定的自定义测试框架（custom_test_frameworks） 功能来替换默认的测试框架。该功能不需要额外的库，因此在<code>#[no_std]</code>环境中它也可以工作。它的工作原理是收集所有标注了<code>#[test_case]</code>属性的函数，然后将这个测试函数的列表作为参数传递给用户指定的runner函数。因此，它实现了对测试过程的最大控制。</p>
<p>与默认的测试框架相比，它的缺点是有一些高级功能诸如<code>should_panic tests</code>都不可用了。相对的，如果需要这些功能，我们需要自己来实现。当然，这点对我们来说是好事，因为我们的环境非常特殊，在这个环境里，这些高级功能的默认实现无论如何都是无法工作的，举个例子，<code>#[should_panic]</code>属性依赖于堆栈展开来捕获内核<code>panic</code>，而我的内核早已将其禁用了。</p>
<p>要为我们的内核实现自定义测试框架，我们需要将如下代码添加到我们的<code>main.rs</code>中去:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// src/main.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#![feature(custom_test_frameworks)]</span></span><br><span class="line"><span class="meta">#![test_runner(crate::test_runner)]</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#[cfg(test)]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">test_runner</span>(tests: &amp;[&amp;<span class="keyword">dyn</span> <span class="title function_ invoke__">Fn</span>()]) &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Running &#123;&#125; tests&quot;</span>, tests.<span class="title function_ invoke__">len</span>());</span><br><span class="line">    <span class="keyword">for</span> <span class="variable">test</span> <span class="keyword">in</span> tests &#123;</span><br><span class="line">        <span class="title function_ invoke__">test</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们的<code>runner</code>会打印一个简短的<code>debug</code>信息然后调用列表中的每个测试函数。参数类型<code>&amp;[&amp;dyn Fn()]</code>是<code>Fn() trait</code>的<code>trait object</code>引用的一个<code>slice</code>。它基本上可以被看做一个可以像函数一样被调用的类型的引用列表。由于这个函数在不进行测试的时候没有什么用，这里我们使用<code>#[cfg(test)]</code>属性保证它只会出现在测试中。</p>
<p>现在当我们运行<code>cargo xtest</code>，我们可以发现运行成功了。然而，我们看到的仍然是<code>“Hello World“</code>而不是我们的<code>test_runner</code>传递来的信息。这是由于我们的入口点仍然是<code>_start</code>函数——自定义测试框架会生成一个<code>main</code>函数来调用<code>test_runner</code>，但是由于我们使用了<code>#[no_main]</code>并提供了我们自己的入口点，所以这个<code>main</code>函数就被忽略了。</p>
<p>为了修复这个问题，我们需要通过<code>reexport_test_harness_main</code>属性来将生成的函数的名称更改为与<code>main</code>不同的名称。然后我们可以在我们的<code>_start</code>函数里调用这个重命名的函数:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// src/main.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#![reexport_test_harness_main = <span class="string">&quot;test_main&quot;</span>]</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#[no_mangle]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">_start</span>() <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Hello World&#123;&#125;&quot;</span>, <span class="string">&quot;!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="meta">#[cfg(test)]</span></span><br><span class="line">    <span class="title function_ invoke__">test_main</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">loop</span> &#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们将测试框架的入口函数的名字设置为<code>test_main</code>，并在我们的<code>_start</code>入口点里调用它。通过使用条件编译（conditional compilation），我们能够只在上下文环境为测试（test）时调用<code>test_main</code>，因为该函数将不在非测试上下文中生成。</p>
<p>现在当我们执行<code>cargo xtest</code>时，我们可以看到我们的<code>test_runner</code>将<code>“Running 0 tests“</code>信息显示在屏幕上了。我们可以创建第一个测试函数了:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// src/main.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#[test_case]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">trivial_assertion</span>() &#123;</span><br><span class="line">    <span class="built_in">print!</span>(<span class="string">&quot;trivial assertion... &quot;</span>);</span><br><span class="line">    <span class="built_in">assert_eq!</span>(<span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;[ok]&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>传递给<code>test_runner</code>函数的<code>tests</code>切片里包含了一个<code>trivial_assertion</code>函数的引用，从屏幕上输出的<code>trivial assertion... [ok]</code>信息可见，我们的测试已被调用并且顺利通过。</p>
<p>在执行完<code>tests</code>后，<code>test_runner</code>会将结果返回给<code>test_main</code>函数，而这个函数又返回到 <code>_start</code>入口点函数——这样我们就进入了一个死循环，因为入口点函数是不允许返回的。这将导致一个问题：我们希望<code>cargo xtest</code>在所有的测试运行完毕后，才返回并退出。</p></div><div class="tab-pane" id="rtest-2"><p>现在我们在<code>_start</code>函数结束后进入了一个死循环，所以每次执行完<code>cargo xtest</code>后我们都需要手动去关闭<code>QEMU</code>；但是我们还想在没有用户交互的脚本环境下执行<code>cargo xtest</code>。解决这个问题的最佳方式，是实现一个合适的方法来关闭我们的操作系统——不幸的是，这个方式实现起来相对有些复杂，因为这要求我们实现对<code>APM</code>或<code>ACPI</code>电源管理标准的支持。</p>
<p>幸运的是，还有一个绕开这些问题的办法：<code>QEMU</code>支持一种名为<code>isa-debug-exit</code>的特殊设备，它提供了一种从客户系统（guest system）里退出QEMU的简单方式。为了使用这个设备，我们需要向QEMU传递一个<code>-device</code>参数。当然，我们也可以通过将<code>package.metadata.bootimage.test-args</code>配置关键字添加到我们的<code>Cargo.toml</code>来达到目的：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\ in Cargo.toml</span></span><br><span class="line"></span><br><span class="line"><span class="section">[package.metadata.bootimage]</span></span><br><span class="line"><span class="attr">test-args</span> = [<span class="string">&quot;-device&quot;</span>, <span class="string">&quot;isa-debug-exit,iobase=0xf4,iosize=0x04&quot;</span>]</span><br></pre></td></tr></table></figure>

<p><code>bootimage runner</code>会在QEMU的默认测试命令后添加<code>test-args</code>参数。（对于<code>cargo xrun</code>命令，这个参数会被忽略。）</p>
<p>在传递设备名 (<code>isa-debug-exit</code>)的同时，我们还传递了两个参数，<code>iobase</code> 和 <code>iosize</code> 。这两个参数指定了一个<code>I/O 端口</code>，我们的内核将通过它来访问设备。</p>
<p><strong>I&#x2F;O 端口</strong></p>
<p>在<code>x86</code>平台上，CPU和外围硬件通信通常有两种方式，内存映射I&#x2F;O和端口映射I&#x2F;O。之前，我们已经使用内存映射的方式，通过内存地址<code>0xb8000</code>访问了[VGA文本缓冲区]。该地址并没有映射到RAM，而是映射到了VGA设备的一部分内存上。</p>
<p>与内存映射不同，端口映射I&#x2F;O使用独立的I&#x2F;O总线来进行通信。每个外围设备都有一个或数个端口号。CPU采用了特殊的<code>in</code>和<code>out</code>指令来和端口通信，这些指令要求一个端口号和一个字节的数据作为参数（有些这种指令的变体也允许发送<code>u16</code>或是<code>u32</code>长度的数据）。</p>
<p><code>isa-debug-exit</code>设备使用的就是端口映射I&#x2F;O。其中， <code>iobase</code> 参数指定了设备对应的端口地址（在<code>x86</code>中，<code>0xf4</code>是一个通常未被使用的端口），而<code>iosize</code>则指定了端口的大小（<code>0x04</code>代表4字节）。</p>
<p><strong>使用退出(Exit)设备</strong></p>
<p><code>isa-debug-exit</code>设备的功能非常简单。当一个<code>value</code>写入<code>iobase</code>指定的端口时，它会导致QEMU以退出状态（<code>exit status</code>）<code>(value &lt;&lt; 1) | 1</code> 退出。也就是说，当我们向端口写入<code>0</code>时，QEMU将以退出状态<code>(0 &lt;&lt; 1) | 1 = 1</code>退出，而当我们向端口写入<code>1</code>时，它将以退出状态<code>(1 &lt;&lt; 1) | 1 = 3</code>退出。</p>
<p>这里我们使用<code>x86_64 crate</code>提供的 抽象，而不是手动调用<code>in</code>或<code>out</code>指令。为了添加对该<code>crate</code>的依赖，我们可以将其添加到我们的<code>Cargo.toml</code>中的<code>dependencies</code>小节中去：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\ in Cargo.toml</span></span><br><span class="line"></span><br><span class="line"><span class="section">[dependencies]</span></span><br><span class="line"><span class="attr">x86_64</span> = <span class="string">&quot;0.14.2&quot;</span></span><br></pre></td></tr></table></figure>



<p>现在我们可以使用<code>crate</code>中提供的<code>Port</code>类型来创建一个<code>exit_qemu</code>函数了:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in src/main.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#[derive(Debug, Clone, Copy, PartialEq, Eq)]</span></span><br><span class="line"><span class="meta">#[repr(u32)]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">enum</span> <span class="title class_">QemuExitCode</span> &#123;</span><br><span class="line">    Success = <span class="number">0x10</span>,</span><br><span class="line">    Failed = <span class="number">0x11</span>,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">exit_qemu</span>(exit_code: QemuExitCode) &#123;</span><br><span class="line">    <span class="keyword">use</span> x86_64::instructions::port::Port;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">unsafe</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">port</span> = Port::<span class="title function_ invoke__">new</span>(<span class="number">0xf4</span>);</span><br><span class="line">        port.<span class="title function_ invoke__">write</span>(exit_code <span class="keyword">as</span> <span class="type">u32</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<p>该函数在<code>0xf4</code>处创建了一个新的端口，该端口同时也是 <code>isa-debug-exit</code> 设备的 <code>iobase</code> 。然后它会向端口写入传递的退出代码。这里我们使用<code>u32</code>来传递数据，因为我们之前已经将 <code>isa-debug-exit</code>设备的 <code>iosize</code> 指定为4字节了。上述两个操作都是<code>unsafe</code>的，因为I&#x2F;O端口的写入操作通常会导致一些不可预知的行为。</p>
<p>为了指定退出状态，我们创建了一个 <code>QemuExitCode</code>枚举。思路大体上是，如果所有的测试均成功，就以成功退出码退出；否则就以失败退出码退出。这个枚举类型被标记为 <code>#[repr(u32)]</code>，代表每个变量都是一个<code>u32</code>的整数类型。我们使用退出代码<code>0x10</code>代表成功，<code>0x11</code>代表失败。 实际的退出代码并不重要，只要它们不与QEMU的默认退出代码冲突即可。 例如，使用退出代码0表示成功可能并不是一个好主意，因为它在转换后就变成了<code>(0 &lt;&lt; 1) | 1 = 1</code> ，而<code>1</code>是QEMU运行失败时的默认退出代码。 这样，我们就无法将QEMU错误与成功的测试运行区分开来了。</p>
<p>现在我们来更新<code>test_runner</code>的代码，让程序在运行所有测试完毕后退出QEMU：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">test_runner</span>(tests: &amp;[&amp;<span class="keyword">dyn</span> <span class="title function_ invoke__">Fn</span>()]) &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Running &#123;&#125; tests&quot;</span>, tests.<span class="title function_ invoke__">len</span>());</span><br><span class="line">    <span class="keyword">for</span> <span class="variable">test</span> <span class="keyword">in</span> tests &#123;</span><br><span class="line">        <span class="title function_ invoke__">test</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/// new</span></span><br><span class="line">    <span class="title function_ invoke__">exit_qemu</span>(QemuExitCode::Success);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当我们现在运行<code>cargo xtest</code>时，QEMU会在测试运行后立刻退出。现在的问题是，即使我们传递了表示成功（<code>Success</code>）的退出代码， <code>cargo test</code>依然会将所有的测试都视为失败：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&gt; cargo xtest</span><br><span class="line">    Finished dev [unoptimized + debuginfo] target(s) <span class="keyword">in</span> 0.03s</span><br><span class="line">     Running target/x86_64-blog_os/debug/deps/blog_os-5804fc7d2dd4c9be</span><br><span class="line">Building bootloader</span><br><span class="line">   Compiling bootloader v0.5.3 (/home/philipp/Documents/bootloader)</span><br><span class="line">    Finished release [optimized + debuginfo] target(s) <span class="keyword">in</span> 1.07s</span><br><span class="line">Running: `qemu-system-x86_64 -drive format=raw,file=/…/target/x86_64-blog_os/debug/</span><br><span class="line">    deps/bootimage-blog_os-5804fc7d2dd4c9be.bin -device isa-debug-exit,iobase=0xf4,</span><br><span class="line">    iosize=0x04`</span><br><span class="line">error: <span class="built_in">test</span> failed, to rerun pass <span class="string">&#x27;--bin blog_os&#x27;</span></span><br></pre></td></tr></table></figure>

<p>这里的问题在于，<code>cargo test</code>会将所有非<code>0</code>的错误码都视为测试失败。</p>
<p>为了解决这个问题， <code>bootimage</code>提供了一个 <code>test-success-exit-code</code>配置项，可以将指定的退出代码映射到退出代码0:</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[package.metadata.bootimage]</span></span><br><span class="line"><span class="attr">test-args</span> = […]</span><br><span class="line"><span class="attr">test-success-exit-code</span> = <span class="number">33</span>         <span class="comment"># (0x10 &lt;&lt; 1) | 1</span></span><br></pre></td></tr></table></figure>

<p>有了这个配置，<code>bootimage</code>就会将我们的成功退出码映射到退出码<code>0</code>；这样一来， <code>cargo xtest</code>就能正确的识别出测试成功的情况，而不会将其视为测试失败。</p>
<p>我们的测试<code>runner</code>现在会在正确报告测试结果后自动关闭QEMU。我们可以看到QEMU的窗口只会显示很短的时间——我们不容易看清测试的结果。如果测试结果会打印在控制台上而不是QEMU里，让我们能在QEMU退出后仍然能看到测试结果就好了。</p></div><div class="tab-pane" id="rtest-3"><p>要在控制台上查看测试输出，我们需要以某种方式将数据从内核发送到宿主系统。 有多种方法可以实现这一点，例如通过TCP网络接口来发送数据。但是，设置网络堆栈是一项很复杂的任务——这里我们选择更简单的解决方案。</p>
<p><strong>串口</strong></p>
<p>发送数据的一个简单的方式是通过<a href="https://en.wikipedia.org/wiki/Serial_port">串行端口</a>，这是一个现代电脑中已经不存在的旧标准接口（译者注：玩过单片机的同学应该知道，其实译者上大学的时候有些同学的笔记本电脑还有串口的，没有串口的同学在烧录单片机程序的时候也都会需要usb转串口线，一般是51，像stm32有st-link，这个另说，不过其实也可以用串口来下载）。串口非常易于编程，QEMU可以将通过串口发送的数据重定向到宿主机的标准输出或是文件中。</p>
<p>用来实现串行接口的芯片被称为 <a href="https://en.wikipedia.org/wiki/Universal_asynchronous_receiver-transmitter">UARTs</a>。在x86上，有<a href="https://en.wikipedia.org/wiki/Universal_asynchronous_receiver-transmitter#UART_models">很多UART模型</a>，但是幸运的是，它们之间仅有的那些不同之处都是我们用不到的高级功能。目前通用的UARTs都会兼容<a href="https://en.wikipedia.org/wiki/16550_UART">16550 UART</a>，所以我们在我们测试框架里采用该模型。</p>
<p>我们使用<a href="https://docs.rs/uart_16550"><code>uart_16550</code></a> crate来初始化UART，并通过串口来发送数据。为了将该crate添加为依赖，我们将我们的<code>Cargo.toml</code>和<code>main.rs</code>修改为如下:</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\ in Cargo.toml</span></span><br><span class="line"></span><br><span class="line"><span class="section">[dependencies]</span></span><br><span class="line"><span class="attr">uart_16550</span> = <span class="string">&quot;0.2.0&quot;</span></span><br></pre></td></tr></table></figure>

<p><code>uart_16550 crate</code> 包含了一个代表<code>UART</code>寄存器的<code>SerialPort</code>结构体，但是我们任然需要自己来创建一个相应的实例。我们使用以下内容来创建一个新的串口模块 <code>serial</code>：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in src/main.rs</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">mod</span> serial;</span><br></pre></td></tr></table></figure>


<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in src/serial.rs</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> uart_16550::SerialPort;</span><br><span class="line"><span class="keyword">use</span> spin::Mutex;</span><br><span class="line"><span class="keyword">use</span> lazy_static::lazy_static;</span><br><span class="line"></span><br><span class="line">lazy_static! &#123;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">static</span> <span class="keyword">ref</span> SERIAL1: Mutex&lt;SerialPort&gt; = &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">serial_port</span> = <span class="keyword">unsafe</span> &#123; SerialPort::<span class="title function_ invoke__">new</span>(<span class="number">0x3F8</span>) &#125;;</span><br><span class="line">        serial_port.<span class="title function_ invoke__">init</span>();</span><br><span class="line">        Mutex::<span class="title function_ invoke__">new</span>(serial_port)</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>就像<code>VGA</code>文本缓冲区一样，我们使用 <code>lazy_static</code> 和一个自旋锁来创建一个 <code>static writer</code>实例。通过使用 <code>lazy_static</code> ，我们可以保证<code>init</code>方法只会在该示例第一次被使用使被调用。</p>
<p>和 <code>isa-debug-exit</code>设备一样，<code>UART</code>也是用过I&#x2F;O端口进行编程的。由于<code>UART</code>相对来讲更加复杂，它使用多个I&#x2F;O端口来对不同的设备寄存器进行编程。不安全的<code>SerialPort::new</code>函数需要<code>UART</code>的第一个I&#x2F;O端口的地址作为参数，从该地址中可以计算出所有所需端口的地址。我们传递的端口地址为<code>0x3F8</code> ，该地址是第一个串行接口的标准端口号。</p>
<p>为了使串口更加易用，我们添加了 <code>serial_print!</code> 和 <code>serial_println!</code>宏:</p>
<figure class="highlight rust"><figcaption><span>line_numebr:false</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="meta">#[doc(hidden)]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">_print</span>(args: ::core::fmt::Arguments) &#123;</span><br><span class="line">    <span class="keyword">use</span> core::fmt::Write;</span><br><span class="line">    SERIAL1.<span class="title function_ invoke__">lock</span>().<span class="title function_ invoke__">write_fmt</span>(args).<span class="title function_ invoke__">expect</span>(<span class="string">&quot;Printing to serial failed&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// Prints to the host through the serial interface.</span></span><br><span class="line"><span class="meta">#[macro_export]</span></span><br><span class="line"><span class="built_in">macro_rules!</span> serial_print &#123;</span><br><span class="line">    ($($arg:tt)*) =&gt; &#123;</span><br><span class="line">        $crate::serial::_print(<span class="built_in">format_args!</span>($($arg)*));</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// Prints to the host through the serial interface, appending a newline.</span></span><br><span class="line"><span class="meta">#[macro_export]</span></span><br><span class="line"><span class="built_in">macro_rules!</span> serial_println &#123;</span><br><span class="line">    () =&gt; ($crate::serial_print!(<span class="string">&quot;\n&quot;</span>));</span><br><span class="line">    ($fmt:expr) =&gt; ($crate::serial_print!(<span class="built_in">concat!</span>($fmt, <span class="string">&quot;\n&quot;</span>)));</span><br><span class="line">    ($fmt:expr, $($arg:tt)*) =&gt; ($crate::serial_print!(</span><br><span class="line">        <span class="built_in">concat!</span>($fmt, <span class="string">&quot;\n&quot;</span>), $($arg)*));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>该实现和我们此前的<code>print</code>和<code>println</code>宏的实现非常类似。 由于<code>SerialPort</code>类型已经实现了<code>fmt::Write</code> trait，所以我们不需要提供我们自己的实现了。</p>
<p>现在我们可以从测试代码里向串行接口打印而不是向VGA文本缓冲区打印了:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in src/main.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#[cfg(test)]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">test_runner</span>(tests: &amp;[&amp;<span class="keyword">dyn</span> <span class="title function_ invoke__">Fn</span>()]) &#123;</span><br><span class="line">    serial_println!(<span class="string">&quot;Running &#123;&#125; tests&quot;</span>, tests.<span class="title function_ invoke__">len</span>());</span><br><span class="line">    […]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[test_case]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">trivial_assertion</span>() &#123;</span><br><span class="line">    serial_print!(<span class="string">&quot;trivial assertion... &quot;</span>);</span><br><span class="line">    <span class="built_in">assert_eq!</span>(<span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line">    serial_println!(<span class="string">&quot;[ok]&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意，由于我们使用了 <code>#[macro_export]</code> 属性， <code>serial_println</code>宏直接位于根命名空间下——所以通过<code>use crate::serial::serial_println</code> 来导入该宏是不起作用的。</p>
<p><strong>QEMU参数</strong></p>
<p>为了查看QEMU的串行输出，我们需要使用<code>-serial</code>参数将输出重定向到<code>stdout</code>：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\ in Cargo.toml</span></span><br><span class="line"></span><br><span class="line"><span class="section">[package.metadata.bootimage]</span></span><br><span class="line"><span class="attr">test-args</span> = [</span><br><span class="line">    <span class="string">&quot;-device&quot;</span>, <span class="string">&quot;isa-debug-exit,iobase=0xf4,iosize=0x04&quot;</span>, <span class="string">&quot;-serial&quot;</span>, <span class="string">&quot;stdio&quot;</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>现在，当我们运行 <code>cargo xtest</code>时，我们可以直接在控制台里看到测试输出了:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&gt; cargo xtest</span><br><span class="line">    Finished dev [unoptimized + debuginfo] target(s) <span class="keyword">in</span> 0.02s</span><br><span class="line">     Running target/x86_64-blog_os/debug/deps/blog_os-7b7c37b4ad62551a</span><br><span class="line">Building bootloader</span><br><span class="line">    Finished release [optimized + debuginfo] target(s) <span class="keyword">in</span> 0.02s</span><br><span class="line">Running: `qemu-system-x86_64 -drive format=raw,file=/…/target/x86_64-blog_os/debug/</span><br><span class="line">    deps/bootimage-blog_os-7b7c37b4ad62551a.bin -device</span><br><span class="line">    isa-debug-exit,iobase=0xf4,iosize=0x04 -serial stdio`</span><br><span class="line">Running 1 tests</span><br><span class="line">trivial assertion... [ok]</span><br></pre></td></tr></table></figure>

<p>然而，当测试失败时，我们仍然会在QEMU内看到输出结果，因为我们的<code>panic handler</code>还是用了<code>println</code>。为了模拟这个过程，我们将我们的 <code>trivial_assertion test</code>中的断言(assertion)修改为 <code>assert_eq!(0, 1)</code></p>
<p>通过实验可以看到，<code>panic</code>信息被打印到了VGA缓冲区里，而测试输出则被打印到串口上了。<code>panic</code>信息非常有用，所以我们希望能够在控制台中来查看它。</p>
<p><strong>在panic时打印一个错误信息</strong></p>
<p>为了在panic时使用错误信息来退出QEMU，我们可以使用条件编译（conditional compilation）在测试模式下使用（与非测试模式下）不同的panic处理方式:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// our existing panic handler</span></span><br><span class="line"><span class="meta">#[cfg(not(test))]</span> <span class="comment">// new attribute</span></span><br><span class="line"><span class="meta">#[panic_handler]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">panic</span>(info: &amp;PanicInfo) <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, info);</span><br><span class="line">    <span class="keyword">loop</span> &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// our panic handler in test mode</span></span><br><span class="line"><span class="meta">#[cfg(test)]</span></span><br><span class="line"><span class="meta">#[panic_handler]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">panic</span>(info: &amp;PanicInfo) <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    serial_println!(<span class="string">&quot;[failed]\n&quot;</span>);</span><br><span class="line">    serial_println!(<span class="string">&quot;Error: &#123;&#125;\n&quot;</span>, info);</span><br><span class="line">    <span class="title function_ invoke__">exit_qemu</span>(QemuExitCode::Failed);</span><br><span class="line">    <span class="keyword">loop</span> &#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在我们的测试<code>panic</code>处理中，我们用 <code>serial_println</code>来代替<code>println</code> 并使用失败代码来退出QEMU。注意，在<code>exit_qemu</code>调用后，我们仍然需要一个无限循环的<code>loop</code>因为编译器并不知道 <code>isa-debug-exit</code>设备会导致程序退出。</p>
<p>现在，即使在测试失败的情况下QEMU仍然会存在，并会将一些有用的错误信息打印到控制台：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&gt; cargo xtest</span><br><span class="line">    Finished dev [unoptimized + debuginfo] target(s) <span class="keyword">in</span> 0.02s</span><br><span class="line">     Running target/x86_64-blog_os/debug/deps/blog_os-7b7c37b4ad62551a</span><br><span class="line">Building bootloader</span><br><span class="line">    Finished release [optimized + debuginfo] target(s) <span class="keyword">in</span> 0.02s</span><br><span class="line">Running: `qemu-system-x86_64 -drive format=raw,file=/…/target/x86_64-blog_os/debug/</span><br><span class="line">    deps/bootimage-blog_os-7b7c37b4ad62551a.bin -device</span><br><span class="line">    isa-debug-exit,iobase=0xf4,iosize=0x04 -serial stdio`</span><br><span class="line">Running 1 tests</span><br><span class="line">trivial assertion... [failed]</span><br><span class="line"></span><br><span class="line">Error: panicked at <span class="string">&#x27;assertion failed: `(left == right)`</span></span><br><span class="line"><span class="string">  left: `0`,</span></span><br><span class="line"><span class="string"> right: `1`&#x27;</span>, src/main.rs:65:5</span><br></pre></td></tr></table></figure>

<p>由于现在所有的测试都将输出到控制台上，我们不再需要让QEMU窗口弹出一小会儿了——我们完全可以把窗口藏起来。</p>
<p><strong>隐藏QEMU</strong></p>
<p>由于我们使用<code>isa-debug-exit</code>设备和串行端口来报告完整的测试结果，所以我们不再需要QMEU的窗口了。我们可以通过向QEMU传递 <code>-display none</code>参数来将其隐藏:</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\ in Cargo.toml</span></span><br><span class="line"></span><br><span class="line"><span class="section">[package.metadata.bootimage]</span></span><br><span class="line"><span class="attr">test-args</span> = [</span><br><span class="line">    <span class="string">&quot;-device&quot;</span>, <span class="string">&quot;isa-debug-exit,iobase=0xf4,iosize=0x04&quot;</span>, <span class="string">&quot;-serial&quot;</span>, <span class="string">&quot;stdio&quot;</span>,</span><br><span class="line">    <span class="string">&quot;-display&quot;</span>, <span class="string">&quot;none&quot;</span></span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>现在QEMU完全在后台运行且没有任何窗口会被打开。这不仅不那么烦人，还允许我们的测试框架在没有图形界面的环境里，诸如CI服务器或是<a href="https://en.wikipedia.org/wiki/Secure_Shell">SSH</a>连接里运行。</p>
<p><strong>超时</strong></p>
<p>由于 <code>cargo xtest</code> 会等待<code>test runner</code>退出，如果一个测试永远不返回那么它就会一直阻塞<code>test runner</code>。幸运的是，在实际应用中这并不是一个大问题，因为无限循环通常是很容易避免的。在我们的这个例子里，无限循环会发生在以下几种不同的情况中：</p>
<ul>
<li>bootloader加载内核失败，导致系统不停重启；</li>
<li>BIOS&#x2F;UEFI固件加载bootloader失败，同样会导致无限重启；</li>
<li>CPU在某些函数结束时进入一个loop {}语句，例如因为QEMU的exit设备无法正常工作而导致死循环；</li>
<li>硬件触发了系统重置，例如未捕获CPU异常时（后续的文章将会详细解释）。</li>
</ul>
<p>由于无限循环可能会在各种情况中发生，因此，<code> bootimage</code> 工具默认为每个可执行测试设置了一个长度为5分钟的超时时间。如果测试未在此时间内完成，则将其标记为失败，并向控制台输出“Timed Out（超时）“错误。这个功能确保了那些卡在无限循环里的测试不会一直阻塞<code>cargo xtest</code>。</p>
<p>你可以将<code>loop {}</code>语句添加到 <code>trivial_assertion</code>测试中来进行尝试。当你运行 <code>cargo xtest</code>时，你可以发现该测试会在五分钟后被标记为超时。超时持续的时间可以通过<code>Cargo.toml</code>中的<code>test-timeout</code>来进行配置：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment">#\ in Cargo.toml</span></span><br><span class="line"></span><br><span class="line"><span class="section">[package.metadata.bootimage]</span></span><br><span class="line"><span class="attr">test-timeout</span> = <span class="number">300</span>          <span class="comment"># (in seconds)</span></span><br></pre></td></tr></table></figure>

<p>如果你不想为了观察<code>trivial_assertion</code> 测试超时等待5分钟之久，你可以暂时降低将上述值。</p>
<p>此后，我们不再需要 <code>trivial_assertion</code> 测试，所以我们可以将其删除。</p>
<p><strong>测试VGA缓冲区</strong></p>
<p>现在我们已经有了一个可以工作的测试框架了，我们可以为我们的VGA缓冲区实现创建一些测试。首先，我们创建了一个非常简单的测试来验证 <code>println</code>是否正常运行而不会<code>panic</code>：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="meta">#[cfg(test)]</span></span><br><span class="line"><span class="keyword">use</span> crate::&#123;serial_print, serial_println&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[test_case]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">test_println_simple</span>() &#123;</span><br><span class="line">    serial_print!(<span class="string">&quot;test_println... &quot;</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;test_println_simple output&quot;</span>);</span><br><span class="line">    serial_println!(<span class="string">&quot;[ok]&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个测试所做的仅仅是将一些内容打印到VGA缓冲区。如果它正常结束并且没有panic，也就意味着<code>println</code>调用也没有panic。由于我们只需要将 <code>serial_println</code> 导入到测试模式里，所以我们添加了 <code>cfg(test)</code> 属性（attribute）来避免正常模式下 <code>cargo xbuild</code>会出现的未使用导入警告（unused import warning）。</p>
<p>为了确保即使打印很多行且有些行超出屏幕的情况下也没有panic发生，我们可以创建另一个测试：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in src/vga_buffer.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#[test_case]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">test_println_many</span>() &#123;</span><br><span class="line">    serial_print!(<span class="string">&quot;test_println_many... &quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> <span class="variable">_</span> <span class="keyword">in</span> <span class="number">0</span>..<span class="number">200</span> &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;test_println_many output&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    serial_println!(<span class="string">&quot;[ok]&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们还可以创建另一个测试函数，来验证打印的几行字符是否真的出现在了屏幕上:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in src/vga_buffer.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#[test_case]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">test_println_output</span>() &#123;</span><br><span class="line">    serial_print!(<span class="string">&quot;test_println_output... &quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">s</span> = <span class="string">&quot;Some test string that fits on a single line&quot;</span>;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, s);</span><br><span class="line">    <span class="title function_ invoke__">for</span> (i, c) <span class="keyword">in</span> s.<span class="title function_ invoke__">chars</span>().<span class="title function_ invoke__">enumerate</span>() &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">screen_char</span> = WRITER.<span class="title function_ invoke__">lock</span>().buffer.chars[BUFFER_HEIGHT - <span class="number">2</span>][i].<span class="title function_ invoke__">read</span>();</span><br><span class="line">        <span class="built_in">assert_eq!</span>(char::<span class="title function_ invoke__">from</span>(screen_char.ascii_character), c);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    serial_println!(<span class="string">&quot;[ok]&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该函数定义了一个测试字符串，并通过 <code>println</code>将其输出，然后遍历静态 <code>WRITER</code>也就是vga字符缓冲区的屏幕字符。由于<code>println</code>在将字符串打印到屏幕上最后一行后会立刻附加一个新行(即输出完后有一个换行符)，所以这个字符串应该会出现在第 <code>BUFFER_HEIGHT - 2</code>行。</p>
<p>通过使用<a href="https://doc.rust-lang.org/core/iter/trait.Iterator.html#method.enumerate"><code>enumerate</code></a> ，我们统计了变量<code>i</code>的迭代次数，然后用它来加载对应于<code>c</code>的屏幕字符。 通过比较屏幕字符的<code>ascii_character</code>和<code>c</code> ，我们可以确保字符串的每个字符确实出现在vga文本缓冲区中。</p>
<p>如你所想，我们可以创建更多的测试函数：例如一个用来测试当打印一个很长的且包装正确的行时是否会发生panic的函数，或是一个用于测试换行符、不可打印字符、非unicode字符是否能被正确处理的函数。</p>
<p>在这篇文章的剩余部分，我们还会解释如何创建一个_集成测试_以测试不同组建之间的交互。</p></div><div class="tab-pane" id="rtest-4"><p>在Rust中，集成测试（integration tests）的约定是将其放到项目根目录中的<code>tests</code>目录下(即<code>src</code>的同级目录)。无论是默认测试框架还是自定义测试框架都将自动获取并执行该目录下所有的测试。</p>
<p>所有的集成测试都是它们自己的可执行文件，并且与我们的<code>main.rs</code>完全独立。这也就意味着每个测试都需要定义它们自己的函数入口点。让我们创建一个名为<code>basic_boot</code>的例子来看看集成测试的工作细节吧:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in tests/basic_boot.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#![no_std]</span></span><br><span class="line"><span class="meta">#![no_main]</span></span><br><span class="line"><span class="meta">#![feature(custom_test_frameworks)]</span></span><br><span class="line"><span class="meta">#![test_runner(crate::test_runner)]</span></span><br><span class="line"><span class="meta">#![reexport_test_harness_main = <span class="string">&quot;test_main&quot;</span>]</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> core::panic::PanicInfo;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[no_mangle]</span> <span class="comment">// don&#x27;t mangle the name of this function</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">_start</span>() <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="title function_ invoke__">test_main</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">loop</span> &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">test_runner</span>(tests: &amp;[&amp;<span class="keyword">dyn</span> <span class="title function_ invoke__">Fn</span>()]) &#123;</span><br><span class="line">    <span class="built_in">unimplemented!</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[panic_handler]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">panic</span>(info: &amp;PanicInfo) <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="keyword">loop</span> &#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>由于集成测试都是单独的可执行文件，所以我们需要再次提供所有的<code>crate</code>属性(<code>no_std</code>, <code>no_main</code>, <code>test_runner</code>, 等等)。我们还需要创建一个新的入口点函数<code>_start</code>，用于调用测试入口函数<code>test_main</code>。我们不需要任何的<code>cfg(test) attributes</code>(属性)，因为集成测试的二进制文件在非测试模式下根本不会被编译构建。</p>
<p>这里我们采用<code>unimplemented</code>宏，充当<code>test_runner</code>暂未实现的占位符；添加简单的<code>loop {}</code>循环，作为<code>panic</code>处理器的内容。理想情况下，我们希望能向我们在<code>main.rs</code>里所做的一样使用<code>serial_println</code>宏和<code>exit_qem</code>u函数来实现这个函数。但问题是，由于这些测试的构建和我们的<code>main.rs</code>的可执行文件是完全独立的，我们没有办法使用这些函数。</p>
<p>如果现阶段你运行<code>cargo xtest</code>，你将进入一个无限循环，因为目前<code>panic</code>的处理就是进入无限循环。你需要使用快捷键<code>Ctrl+c</code>，才可以退出QEMU。</p>
<p><strong>创建一个库</strong></p>
<p>为了让这些函数能在我们的集成测试中使用，我们需要从我们的<code>main.rs</code>中分割出一个库，这个库应当可以被其他的crate和集成测试可执行文件使用。为了达成这个目的，我们创建了一个新文件，<code>src/lib.rs</code>:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// src/lib.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#![no_std]</span></span><br></pre></td></tr></table></figure>

<p>和<code>main.rs</code>一样，<code>lib.rs</code>也是一个可以被cargo自动识别的特殊文件。该库是一个独立的编译单元，所以我们需要再次指定<code>#![no_std]</code> 属性。</p>
<p>为了让我们的库可以和<code>cargo xtest</code>一起协同工作，我们还需要移动以下测试函数和属性:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in src/lib.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#![cfg_attr(test, no_main)]</span></span><br><span class="line"><span class="meta">#![feature(custom_test_frameworks)]</span></span><br><span class="line"><span class="meta">#![test_runner(crate::test_runner)]</span></span><br><span class="line"><span class="meta">#![reexport_test_harness_main = <span class="string">&quot;test_main&quot;</span>]</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> core::panic::PanicInfo;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">test_runner</span>(tests: &amp;[&amp;<span class="keyword">dyn</span> <span class="title function_ invoke__">Fn</span>()]) &#123;</span><br><span class="line">    serial_println!(<span class="string">&quot;Running &#123;&#125; tests&quot;</span>, tests.<span class="title function_ invoke__">len</span>());</span><br><span class="line">    <span class="keyword">for</span> <span class="variable">test</span> <span class="keyword">in</span> tests &#123;</span><br><span class="line">        <span class="title function_ invoke__">test</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="title function_ invoke__">exit_qemu</span>(QemuExitCode::Success);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">test_panic_handler</span>(info: &amp;PanicInfo) <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    serial_println!(<span class="string">&quot;[failed]\n&quot;</span>);</span><br><span class="line">    serial_println!(<span class="string">&quot;Error: &#123;&#125;\n&quot;</span>, info);</span><br><span class="line">    <span class="title function_ invoke__">exit_qemu</span>(QemuExitCode::Failed);</span><br><span class="line">    <span class="keyword">loop</span> &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// Entry point for `cargo xtest`</span></span><br><span class="line"><span class="meta">#[cfg(test)]</span></span><br><span class="line"><span class="meta">#[no_mangle]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">_start</span>() <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="title function_ invoke__">test_main</span>();</span><br><span class="line">    <span class="keyword">loop</span> &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[cfg(test)]</span></span><br><span class="line"><span class="meta">#[panic_handler]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">panic</span>(info: &amp;PanicInfo) <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="title function_ invoke__">test_panic_handler</span>(info)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>为了能在可执行文件和集成测试中使用<code>test_runner</code>，我们不对其应用<code>cfg(test)</code> attribute(属性)，并将其设置为public。同时，我们还将panic的处理程序分解为public函数<code>test_panic_handler</code>，这样一来它也可以用于可执行文件了。</p>
<p>由于我们的<code>lib.rs</code>是独立于<code>main.rs</code>进行测试的，因此当该库实在测试模式下编译时我们需要添加一个<code>_start</code>入口点和一个panic处理程序。通过使用<a href="https://doc.rust-lang.org/reference/conditional-compilation.html#the-cfg_attr-attribute"><code>cfg_attr</code></a> ，我们可以在这种情况下有条件地启用<code>no_main</code> 属性。</p>
<p>我们还将<code>QemuExitCode</code>枚举和<code>exit_qemu</code>函数从main.rs移动过来，并将其设置为公有函数：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in src/lib.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#[derive(Debug, Clone, Copy, PartialEq, Eq)]</span></span><br><span class="line"><span class="meta">#[repr(u32)]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">enum</span> <span class="title class_">QemuExitCode</span> &#123;</span><br><span class="line">    Success = <span class="number">0x10</span>,</span><br><span class="line">    Failed = <span class="number">0x11</span>,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">exit_qemu</span>(exit_code: QemuExitCode) &#123;</span><br><span class="line">    <span class="keyword">use</span> x86_64::instructions::port::Port;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">unsafe</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">port</span> = Port::<span class="title function_ invoke__">new</span>(<span class="number">0xf4</span>);</span><br><span class="line">        port.<span class="title function_ invoke__">write</span>(exit_code <span class="keyword">as</span> <span class="type">u32</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>现在，可执行文件和集成测试都可以从库中导入这些函数，而不需要实现自己的定义。为了使<code>println</code> 和 <code>serial_println</code>可用，我们将以下的模块声明代码也移动到<code>lib.rs</code>中：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// in src/lib.rs</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">mod</span> serial;</span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">mod</span> vga_buffer;</span><br></pre></td></tr></table></figure>

<p>我们将这些模块设置为public(公有)，这样一来我们在库的外部也一样能使用它们了。由于这两者都用了该模块内的<code>_print</code>函数，所以这也是让<code>println</code> 和 <code>serial_println</code>宏可用的必要条件。</p>
<p>现在我们修改我们的<code>main.rs</code>代码来使用该库:</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// src/main.rs</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#![no_std]</span></span><br><span class="line"><span class="meta">#![no_main]</span></span><br><span class="line"><span class="meta">#![feature(custom_test_frameworks)]</span></span><br><span class="line"><span class="meta">#![test_runner(blog_os::test_runner)]</span></span><br><span class="line"><span class="meta">#![reexport_test_harness_main = <span class="string">&quot;test_main&quot;</span>]</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> core::panic::PanicInfo;</span><br><span class="line"><span class="keyword">use</span> blog_os::println;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[no_mangle]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="keyword">fn</span> <span class="title function_">_start</span>() <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Hello World&#123;&#125;&quot;</span>, <span class="string">&quot;!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="meta">#[cfg(test)]</span></span><br><span class="line">    <span class="title function_ invoke__">test_main</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">loop</span> &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/// This function is called on panic.</span></span><br><span class="line"><span class="meta">#[cfg(not(test))]</span></span><br><span class="line"><span class="meta">#[panic_handler]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">panic</span>(info: &amp;PanicInfo) <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, info);</span><br><span class="line">    <span class="keyword">loop</span> &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[cfg(test)]</span></span><br><span class="line"><span class="meta">#[panic_handler]</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">panic</span>(info: &amp;PanicInfo) <span class="punctuation">-&gt;</span> ! &#123;</span><br><span class="line">    blog_os::<span class="title function_ invoke__">test_panic_handler</span>(info)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到，这个库用起来就像一个普通的外部<code>crate</code>。它的调用方法与其它<code>crate</code>无异；在我们的这个例子中，位置可能为<code>blog_os</code>。上述代码使用了<code>test_runner attribute</code>中的<code>blog_os::test_runner</code>函数和<code>cfg(test)</code>的panic处理中的<code>blog_os::test_panic_handler</code>函数。它还导入了<code>println</code>宏，这样一来，我们可以在我们的<code>_start</code> 和 <code>panic</code>中使用它了。</p>
<p>与此同时，<code>cargo xrun</code> 和 <code>cargo xtest</code>可以再次正常工作了。当然了，<code>cargo xtes</code>t仍然会进入无限循环（你可以通过<code>ctrl+c</code>来退出）。接下来让我们在我们的集成测试中通过所需要的库函数来修复这个问题吧。</p></div></div></div></div><div class="tab-pane" id="question-3"><p><strong>This is Tab 3.</strong></p>
<div class="tabs" id="sub-tabs"><ul class="nav-tabs"><li class="tab active"><a href="#sub-tabs-1">Sub Tabs 1</a></li><li class="tab"><a href="#sub-tabs-2">Sub Tabs 2</a></li><li class="tab"><a href="#sub-tabs-3">Sub Tabs 3</a></li></ul><div class="tab-content"><div class="tab-pane active" id="sub-tabs-1"><p><strong>This is Sub Tab 1.</strong></p></div><div class="tab-pane" id="sub-tabs-2"></div><div class="tab-pane" id="sub-tabs-3"><div class="tabs" id="sub-sub-tabs"><ul class="nav-tabs"><li class="tab active"><a href="#sub-sub-tabs-1">Sub-Sub Tabs 1</a></li><li class="tab"><a href="#sub-sub-tabs-2">Sub-Sub Tabs 2</a></li><li class="tab"><a href="#sub-sub-tabs-3">Sub-Sub Tabs 3</a></li></ul><div class="tab-content"><div class="tab-pane active" id="sub-sub-tabs-1"></div><div class="tab-pane" id="sub-sub-tabs-2"></div><div class="tab-pane" id="sub-sub-tabs-3"><p><strong>This is Sub-Sub Tab 3 of Sub Tab 3.</strong></p></div></div></div></div></div></div></div></div></div>

<h1 id="一些项目建议"><a href="#一些项目建议" class="headerlink" title="一些项目建议"></a>一些项目建议</h1><h2 id="關於如何參與開源項目"><a href="#關於如何參與開源項目" class="headerlink" title="關於如何參與開源項目"></a>關於如何參與開源項目</h2><blockquote>
<p>這裏僅供參考</p>
</blockquote>
<p>首先，找到對應的issue -&gt; fork官方的倉庫 -&gt; 把自己的fork下來的倉庫clone到相應的開發環境 -&gt; 一般開源提交，先創建一個branch，命名方式推薦 issue-{issue_id} -&gt; 將本地的代碼提交到雲端的倉庫中，並創建相應的 issue-{issu_id} branch -&gt; 解決與上游代碼的衝突， 提交pull request</p>
]]></content>
      <categories>
        <category>os</category>
        <category>camp</category>
        <category>daily</category>
      </categories>
      <tags>
        <tag>camp</tag>
      </tags>
  </entry>
  <entry>
    <title>淺談一下 SmartPtr</title>
    <url>/2022/06/11/code/cpp/loki/SmartPtr/</url>
    <content><![CDATA[<p>關於智能指針，這是一個十分誘人的主題，至少對於C++。對於C++而言，可以這麽説， smart pointers是最普及、最複雜、最强大的工具：其吸引人的地方在於，它涉及語法和語義上的很多議題。</p>
<p>smart pointers 是一種C++對象，它提供<code>operator-&gt;</code>和<strong>unary</strong> <code>operator*</code>， 以此模擬一般指針（simple pointers）的行爲。除了提供指針的語法和語義，smart pointers 通常會在底層完成很多有用的工作（例如内存管理或鎖定操作），這麽以來，對於smart pointers所指對象的生命周期，應用程序就不必特意管理。</p>
<span id="more"></span>


<figure class="highlight bash"><figcaption><span>Import Demo</span></figcaption><table><tr><td class="code"><pre><span class="line">// &#123;\% include_code AllowConversion lang:cpp from:56 to:59 cpp/loki/SmartPtr.cpp \%&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Smart-Pointers-基礎"><a href="#Smart-Pointers-基礎" class="headerlink" title="Smart Pointers 基礎"></a>Smart Pointers 基礎</h2><p>什麽是smart pointer?它是一個C++ class，在語法和某些語義上模擬一般指針，但提供更多功能。由於smart pointers指向“類型各異”之對象，所以他們勢必擁有大量共通代碼，所以現有的高質量smart pointers幾乎都以template完成，template參數為被指物（pointee）之型別，正如以下所見：</p>
<figure class="highlight cpp"><figcaption><span>AllowConversion</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="keyword">class</span> <span class="title class_">SmartPtr</span> &#123;</span><br><span class="line">  <span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">SmartPtr</span><span class="params">(T* pointee)</span>: pointee_(pointee);</span></span><br><span class="line">    SmartPtr&amp; <span class="keyword">operator</span>=(<span class="type">const</span> SmartPtr&amp; other);</span><br><span class="line">    ~<span class="built_in">SmartPtr</span>();</span><br><span class="line marked">    T&amp; <span class="keyword">operator</span>*() <span class="type">const</span> &#123;</span><br><span class="line">      <span class="comment">// ...</span></span><br><span class="line">      <span class="keyword">return</span> *pointee_;</span><br><span class="line">    &#125;</span><br><span class="line marked">    T* <span class="keyword">operator</span>-&gt;() <span class="type">const</span> &#123;</span><br><span class="line">      <span class="comment">// ...</span></span><br><span class="line">      <span class="keyword">return</span> pointee_;</span><br><span class="line">    &#125;</span><br><span class="line">  <span class="keyword">private</span>:</span><br><span class="line">    T* pointee_;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="關於解構的-Policy"><a href="#關於解構的-Policy" class="headerlink" title="關於解構的 Policy"></a>關於解構的 Policy</h2><h3 id="OwnerPolicy"><a href="#OwnerPolicy" class="headerlink" title="OwnerPolicy"></a>OwnerPolicy</h3><h4 id="RefCounted"><a href="#RefCounted" class="headerlink" title="RefCounted"></a>RefCounted</h4><p>引用計數法，此法追蹤</p>
<figure class="highlight cpp"><figcaption><span>RefCounted</span></figcaption><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">P</span>&gt; <span class="keyword">class</span> <span class="title class_">RefCounted</span> &#123;</span><br><span class="line marked">    <span class="type">unsigned</span> <span class="type">int</span>* pCount_;</span><br><span class="line">  <span class="keyword">protected</span>:</span><br><span class="line">    <span class="built_in">RefCounted</span>(): <span class="built_in">pCount_</span>(<span class="keyword">new</span> <span class="type">unsigned</span> <span class="built_in">int</span>(<span class="number">1</span>)) &#123;&#125;</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">IsUnique</span><span class="params">()</span> <span class="type">const</span> </span>&#123; <span class="keyword">return</span> *pCount_ == <span class="number">1</span>; &#125;</span><br><span class="line">    <span class="function">P <span class="title">Clone</span><span class="params">(<span class="type">const</span> P&amp; val)</span> </span>&#123; ++*pCount_; <span class="keyword">return</span> val; &#125;</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">Release</span><span class="params">(<span class="type">const</span> P&amp;)</span> </span>&#123; <span class="keyword">if</span> (!--*pCount_) <span class="keyword">delete</span> pCount_; <span class="keyword">return</span> <span class="literal">false</span>; &#125;</span><br><span class="line">    <span class="function"><span class="type">static</span> <span class="type">void</span> <span class="title">hello</span><span class="params">()</span> </span>&#123; std::cout &lt;&lt; <span class="string">&quot;Hello RefCounted!\n&quot;</span>; &#125;</span><br><span class="line">    <span class="keyword">enum</span> &#123; destructiveCopy = <span class="literal">false</span> &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>cpp</category>
        <category>loki</category>
      </categories>
      <tags>
        <tag>loki</tag>
      </tags>
  </entry>
</search>
